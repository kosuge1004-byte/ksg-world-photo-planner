# Androidホーム画面版の現在地修正

## 原因

- 従来は通常のWebページとして開くための情報しかなく、Androidへ追加した際のアプリ識別情報が不足していた。
- 現在地取得前のPermissions API判定が、ホーム画面版で古い拒否状態を返すと、実際の位置取得を試す前に処理を終了していた。
- 権限変更後に同じ画面から再実行する操作がなかった。

## 修正

- `manifest.webmanifest`、192px/512pxアイコン、Android向けメタ情報を追加した。
- 事前のPermissions API判定で中止せず、`getCurrentPosition`の結果を権限・取得結果の基準にした。
- 高精度取得がタイムアウトした場合は通常精度でもう一度取得する既存のフォールバックを維持した。
- 権限案内へ「再試行」を追加した。
- standalone表示を検出し、Androidホーム画面版に合った権限確認手順を表示するようにした。

## 再テスト手順

1. 修正版をHTTPSで公開する。
2. Androidに残っている以前のホーム画面ショートカットを削除する。
3. Chromeで修正版を開き、ホーム画面へ再追加またはインストールする。
4. 現在地ボタンを押し、位置情報を「アプリの使用中のみ許可」にする。
5. 一度拒否した場合は案内に従って権限を変更し、「再試行」を押す。

旧ショートカットは新しいmanifest情報を保持していないため、再追加後に確認する。
