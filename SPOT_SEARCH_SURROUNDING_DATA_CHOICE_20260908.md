# スポット検索直後の周辺データ3択（2026-09-08）

## 仕様
スポット検索で「被写体」を確定した直後に、以下の3択ダイアログを表示する。

1. ダウンロードしてお気に入りに登録
2. ダウンロードしてお気に入りには登録しない
3. ダウンロードしない

「三脚位置」を検索した場合は、被写体周辺データではないためこのダイアログを表示しない。

## 実装
- 既存の360方位・三脚候補点計算用地形データ保存 (`backfillBearingProfiles`) を再利用。
- ダウンロードのみを選んだ場合も、被写体IDに紐づく端末キャッシュとして保持する。
- お気に入り登録は `addFavoriteSubject()` で冪等に行い、既登録地点を誤って解除しない。
- ダウンロード対象はグローバルな現在被写体ではなく、スポット検索で確定した座標をpending stateへ保持して使う。
- 既存のお気に入り一覧からの「三脚候補データを保存」ダイアログは従来の2択を維持。

## 検証
`node scripts/verify-spot-search-surrounding-download-choice-20260908.mjs`

専用静的回帰テスト 9/9 PASS。
変更ファイルはグローバルTypeScript `transpileModule` で構文確認済み。
完全な `npm run build` はnode_modulesがZIPに含まれないため、この検証環境では未実施。
