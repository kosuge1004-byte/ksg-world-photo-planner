# Phase5-5 API呼び出し削減・総合検証

## 実装
- 最高精度DEM処理で地点ごとに実行していたジオイドAPI GETを、最大512地点のPOSTバッチへ統合。
- 同一内容の未完了バッチは端末側共有Promiseで一本化。
- Cloudflare Functions側でバッチ入力を検証し、R2へ180日キャッシュ。
- 従来の単一点GETは互換性のため維持し、同じR2キャッシュ層へ接続。
- 通信診断へgsi-geoidカテゴリを追加し、回数と所要時間を確認可能にした。

## 効果
最高精度処理でN地点を処理する場合、ジオイド取得のブラウザ→Cloudflare API呼び出しは原則N回から1回へ減る。Cloudflareから国土地理院への取得はサーバーメモリ/R2キャッシュが優先される。

## 制約
- R2利用にはNETWORK_CACHEバインディングが必要。
- 実測の削減率は検索地点数、R2ヒット率、既存端末キャッシュ状態で変わる。
- node_modulesが同梱されていない環境では完全ビルドは実行できない。
