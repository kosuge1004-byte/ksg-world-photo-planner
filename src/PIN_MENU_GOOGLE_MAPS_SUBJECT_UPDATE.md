# ピンメニュー改修

- 「3Dで被写体を指定」をピンメニューから削除
- 「Googleマップへ被写体位置を送る」を追加
- 被写体ピン未配置時はボタンを無効化
- 被写体ピンの緯度・経度を Google Maps 検索URLへ渡す

## 検証
ソース整合性を確認済み。
ビルドは依存パッケージ取得時に実行環境のnpmミラーで `youch-core@0.3.3` が404となり、完了できなかった。
