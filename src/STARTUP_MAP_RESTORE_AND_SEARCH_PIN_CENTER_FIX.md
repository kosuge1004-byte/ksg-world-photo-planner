# 起動時地図復元・検索被写体ピン中央表示 修正

## 変更内容

- 地図中心座標、2Dズーム、2D/3D表示モードを端末の localStorage に保存。
- アプリ再起動時は、最後に開いていた地図中心と表示モードから開始。
- 検索結果から被写体ピンを配置する際、3Dカメラの注視点へ被写体の実標高を使用。
- 高山などで標高0mが注視中心になり、被写体ピンが画面中央から外れる問題を修正。
- 2D表示では検索結果の座標を mapCenter に即時反映。
- 保存履歴が壊れている場合は従来の初期位置へ安全にフォールバック。
