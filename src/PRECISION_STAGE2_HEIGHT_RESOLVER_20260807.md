# 精度最優先 Stage 2: 高度取得経路の統一

- 2D選択、検索、URL、座標入力用の `resolveGroundPoint()` を追加。
- 解決済み高度は楕円体高・標高・ジオイド高・取得元がすべて確定した `ResolvedGroundPoint` として扱う。
- 高度取得失敗時の楕円体高0mフォールバックを三脚・被写体から削除。
- 2D人物配置はDEM高度取得完了後にのみ確定し、線形補間による暫定配置を行わない。
- 3D表面選択の共通化と `pickEllipsoid()` 廃止はStage 3で実施する。
