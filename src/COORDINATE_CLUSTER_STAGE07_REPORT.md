# 修正07 完了報告

## 修正内容

- プレビュー・画角内検索・横切り検索のフレーム内判定を `isCelestialInCameraFrame()` へ統一。
- 現行プレビュー仕様を維持し、太陽・月は円盤の一部がフレームへ重なれば可視、天の川は中心がフレーム内の場合に可視と定義。
- 横切り検索も最終候補時刻で共通フレーム判定を必須化。
- 画角内検索の境界二分補間でも共通フレーム判定を使用。
- 400 / 800 / 1600mmの可変サンプリング間隔を数値検証。

## 対象ファイル

- `src/cesium/celestial.ts`
- `src/search/celestialTransitSearch.ts`
- `scripts/verify-frame-boundaries.mjs`
- `package.json`

## 確認条件

- 画角境界付近で検索とプレビューが同じ判定関数を使用: 満たした
- 400 / 800 / 1600mmでサンプリングが最短通過時間に対して十分細かい: 満たした
- 太陽・月・天の川で明示した共通基準を適用: 満たした
- `createCameraProjection()` / `calculateCelestialHorizontalCoordinates()` の既存経路を維持: 満たした
- 修正01の回帰検証: 満たした
- TypeScriptエラーなし・本番ビルド成功: 満たした

## 未実装として報告した項目

- ハンバーガーメニューに中心／一部／全体の判定方式設定は存在しない。指示に従い追加していない。
- 現行仕様は太陽・月が「円盤一部」、天の川が「中心」。円盤全体判定を選択するUIは未実装。

## 未解決事項

- 実際の端末画面を使った境界位置の目視比較は未確認。共通関数の呼び出し、数値境界、TypeScript、本番ビルド、Android同期で検証。
