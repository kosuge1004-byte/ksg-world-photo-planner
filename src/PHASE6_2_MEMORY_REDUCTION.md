# Phase6-2 メモリ削減

- 地形サンプル取得で、入力点ごとに生成していた `Promise<void>` と `then` クロージャを廃止。
- 座標・詳細度が同じ地点を1要求へ集約し、ユニーク地点数分だけ待機して結果を展開。
- OSM遮蔽候補は巨大なタグ辞書を保持せず、結果に必要な名称だけを保持。
- Adaptive Step距離配列は `Array.from` のコールバックを使わず、事前確保配列へ直接格納。
- LOSのサンプル数、距離、精度閾値、遮蔽判定ロジックは変更していない。
