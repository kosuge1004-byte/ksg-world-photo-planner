# 修正12 完了報告

## 修正内容

- 依存パッケージを追加せず、Node標準テストランナーで本番TypeScriptモジュールを直接実行する回帰テスト基盤を追加しました。
- TypeScriptの拡張子なし相対importをテスト時だけ解決し、TypeScript APIで変換する最小ローダーを追加しました。本番コードはテスト用に変更していません。
- 固定日時・固定座標を使う主要数値テスト8件を追加しました。
- 修正01〜11で追加済みのオフライン検証を統合し、`npm test` 1コマンドで15グループを再現できるようにしました。
- ライブデータ・端末・WebViewへ依存する項目は `REGRESSION_TEST_MANUAL_CHECKLIST.md` へ分離しました。

## 自動テスト対象

| 指示書の最低対象 | テスト内容 | 結果 |
|---|---|---|
| 太陽の視直径 | 2026-03-20の固定値を許容誤差 `1e-8°` で比較 | 満たした |
| 月の視直径 | 同日時の固定値を許容誤差 `1e-8°` で比較 | 満たした |
| 太陽・月の画面座標 | 本番ピンホール投影結果を許容誤差 `1e-7%` で比較 | 満たした |
| センサー寸法 | 3:2・1:1・2:1を許容誤差 `1e-10mm` で比較 | 満たした |
| 水平・垂直画角 | 35mm・3:2の固定値を許容誤差 `1e-9°` で比較 | 満たした |
| `viewCorrection` | 本番共通投影へ方位・高度補正が1回だけ反映されることを比較 | 満たした |
| 屈折補正フォールバック | Bennett補正の固定値と気象取得失敗時の標準補正移行を実行 | 満たした |
| 画角内判定 | 中央・画角外を本番共有判定で確認 | 満たした |
| 横切り検索 | 1日分を実検索し、返却結果が同じカメラ画角内にあることを再確認 | 満たした |
| 検索IDによる旧結果破棄 | スポット検索・日時検索の世代判定配線と競合シナリオを確認 | 満たした |
| 遮蔽状態 | checking・failed・DEM・Google 3Dの確定規則を確認 | 満たした |
| 地平線下判定 | `dem-and-google-3d / below-horizon`だけが確定遮蔽になることを確認 | 満たした |

## 対象ファイル

- `tests/regression/core-calculations.test.mjs`
- `scripts/typescript-source-loader.mjs`
- `scripts/register-typescript-source-loader.mjs`
- `scripts/run-regression-tests.mjs`
- `REGRESSION_TEST_MANUAL_CHECKLIST.md`
- `package.json`

## 確認条件ごとの結果

| 確認条件 | 結果 |
|---|---|
| テストが再現可能 | 満たした。プロジェクト直下の `npm test` で実行可能 |
| 主要計算の回帰を検知できる | 満たした。固定数値8件と既存配線検証14件を15グループへ統合 |
| テスト失敗理由が分かる | 満たした。テスト名、期待値、許容誤差、実値、失敗グループを出力 |
| TypeScriptエラーがない | 満たした。ビルド合格 |
| ビルドが成功する | 満たした |

## 確認結果

- `npm test`: 合格（本番計算8件、統合15グループ）
- `npm run lint`: 合格
- `npm run build`: 合格
- `npm run android:sync`: 合格
- `npm run verify:android`: 合格
- ブラウザで主要画面と天体通過日時検索ダイアログを表示・操作: 合格
- ブラウザのコンソール警告・エラー: 0件

## 未解決事項

- 実際の山・建物・地平線下、Google 3D、DEM、天気APIを使う確認は外部データに依存するため手動確認です。
- Android実機、iPhone Safari、WKWebView、Capacitor iOSは未確認です。確認項目は手動チェックリストに記載しました。
- Google短縮URLのライブ転送確認はネットワーク依存のため `npm test` には含めず、オフラインURL解析を対象にしています。
- 500kBを超える既存JavaScriptチャンクのビルド警告がありますが、ビルドは成功しています。
