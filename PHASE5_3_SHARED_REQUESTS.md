# AstroSight Phase5-3 同一地点共有・リクエスト統合

## 実装

- 未完了リクエストをキー単位で共有する `shareInFlightRequest` を追加。
- タイムゾーン取得を緯度経度小数4桁の共通キーで統合。
- プレビュー側とスポット検索側のタイムゾーン通信を共有。
- OSM地点条件APIを、詳細有無・入力地点列を含む小数5桁キーで統合。
- 同一要求へ参加した呼出側だけをAbort可能にし、他の利用者が共有中の基礎通信は中断しない。
- 成功・失敗のどちらでもin-flightエントリを確実に解除し、失敗結果を残留させない。
- 重複統合はPhase5-1の通信診断へ `deduplicated / shared-in-flight` として記録。

## キー精度

- タイムゾーン: 小数4桁（約11m）。同一撮影地点周辺での重複削減を優先。
- OSM地点条件: 小数5桁（約1.1m）。徒歩可否・道路内外など局所条件の誤共有を抑制。

## 制約

- 共有中の1利用者が中止しても、別利用者のために基礎通信は継続する。
- 完了済みデータの永続共有はPhase5-2端末キャッシュおよび後続Phase5-4のCloudflare/R2が担当する。
