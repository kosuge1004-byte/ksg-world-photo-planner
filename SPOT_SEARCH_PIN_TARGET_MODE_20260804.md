# スポット検索 ピン対象選択

- スポット検索画面に「被写体」「三脚位置」の選択を追加。
- 地名検索とGoogleマップ共有URLのどちらも、選択したピンへ反映。
- 被写体選択時のみ「日時・構図候補も検索」を有効化。
- 三脚位置選択時は同チェックを無効化し、既存の被写体ピンを変更しない。
- 三脚位置検索では日時・構図候補検索を実行しない。
- 被写体の履歴・お気に入り操作は被写体選択時のみ有効。
