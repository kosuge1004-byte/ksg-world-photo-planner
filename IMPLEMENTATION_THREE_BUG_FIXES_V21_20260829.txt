AstroSight v21 - 3件のバグ修正
実装日: 2026-08-29
基準: AstroSight-tripod-reliability-progressive-v20-20260829.zip

対応した報告
1. 手動で被写体ピンを立てる際のメッセージが画面外に表示される
2. 被写体検索（例:「138タワー」）でピンがタワーの頂上ではなく下に立つ
3. 三脚候補点が「確定解なし」で終わることが多い（マニュアルでは候補が見つかる）

---

1. 被写体ピン操作メッセージが画面外に表示される問題

原因: src/App.css の
  .preview-load-status, .app-status { display: none; }
というルールが、廃止済みのプレビュー読込表示(.preview-load-status)と、
setSearchMessage() が書き込む一般ステータスバー(.app-status)を誤って
一緒に隠していた。.app-status にはposition指定も無く、非表示を外しても
フル画面高の3D地図<section>の後ろ（通常の文書フロー最下部）に描画され、
実質的に画面外へ押し出される状態だった。
手動被写体ピン配置（「被写体ピンを配置しました：緯度,経度」等）を含む、
setSearchMessage()を使う全ての案内がこの影響を受けていた。

修正: src/App.css
- .preview-load-status は引き続き非表示のまま（PreviewStatusコンポーネントに
  置き換え済みのため）。
- .app-status は position: fixed で画面下部（env(safe-area-inset-bottom)考慮）
  に収まる独立ルールへ分離。空文字時は .app-status:empty で非表示。

---

2. 被写体検索でピンがタワー頂上でなく下に立つ問題

原因: src/cesium/plateauBuildingVerification.ts の
resolvePlateauRoofGroundPoint() で、局所探索（半径20m以内）で候補が
見つからない場合の広域フォールバック（鉄塔・タワー等、半径30-50m）が、
「最も高い候補」ではなく「候補リストの中で最初に見つかった候補」
（offset→bearing順に機械的に並んだ配列の先頭）を採用していた。
双輪アーチ型の構造物（例:138タワー＝ツインアーチ138）や脚部が広がる
鉄塔では、探索半径内の交点が構造物の低い部分（脚・梁）にも生じるため、
「最初に見つかった」交点がしばしば低い位置になり、頂上ではなく構造物の
下寄りの高さが被写体ピンとして採用されていた。
さらに、広域フォールバックで見つかった候補にはStage3の精密化
（直近だけをより細かく再探索する処理）が適用されておらず、粗い8方位
サンプリングの精度のままだった。

修正: src/cesium/plateauBuildingVerification.ts
- 広域フォールバックも局所探索と同じ highestOf()（最も高い候補）を採用。
- 見つかった頂上候補の由来（局所探索/広域フォールバックいずれか）に
  関わらず、Stage3の精密化を必ず適用するよう統一。
- 探索半径・PLATEAU建物データそのもの・高さ妥当性チェック
  (MAX_PLAUSIBLE_STRUCTURE_HEIGHT_METERS等)は変更していない。

---

3. 三脚候補点が実在するのに「確定解なし」になる問題

前提: v19でrefineWithManualEquivalentProjection()の1次元（距離のみ）探索を
2次元（距離+方位）探索へ修正済み。今回はその上でなお残っていた原因。

原因: 同関数の格子探索が固定3パスで打ち切られ、各パス後の探索半径縮小にも
0.5m未満へは縮まない下限があった。最終判定 ROUND_TRIP_SCREEN_TOLERANCE_
PERCENT（画面比0.5%）は焦点距離に関わらず固定値だが、同じ0.5%が要求する
「実距離での位置精度」は焦点距離が長く（画角が狭く）なるほど厳しくなる。
固定3パス後の格子間隔は約1.5m前後までしか縮まらず、望遠での構図では
0.5%以内に収まる前に探索が尽きていた。人がプレビューを見ながら手動で
ピンをドラッグ調整する場合はこの格子解像度に制約されないため、同じ地点
でも手動なら見つかる、という報告内容と一致する。

修正: src/cesium/tripodCandidates.ts refineWithManualEquivalentProjection()
- パス数の固定上限を3→8（安全上限。無駄な通信を防ぐための上限であり、
  通常は下記の収束条件で先に終了する）。
- 各パスのベストスコアがROUND_TRIP_SCREEN_TOLERANCE_PERCENTの1/5以内に
  収まった時点で、それ以上格子を細かくしても最終判定が変わらないため
  早期終了する収束条件を追加（画角が広く元々精度が出やすいケースでは
  従来通り早期に終わり、DEM通信量は増えない）。
- 探索半径縮小の下限を0.5m→0.025m（格子間隔換算で約5cm）に緩和し、
  望遠構図で必要なセンチメートル単位の位置精度まで格子を縮小できるように
  した。
- CameraModel投影経路・0.5%round-trip判定・DEM解像度（1m固定）・
  収束角度条件・wide fallback・double-checkは変更していない。

---

変更していない精度条件（全修正共通）
- GSI DEM1A -> DEM5A -> DEM5B -> DEM5C -> DEM10Bの優先順位
- PLATEAU建物データそのもの、高さ妥当性の上限(1000m)
- CameraModel round-trip 0.5% dx/dy判定
- 収束角度条件(CONVERGED_HORIZONTAL_DEGREES=0.002)

検証
- npx esbuild によるTypeScript構文チェック: PASS
  (src/cesium/tripodCandidates.ts, src/cesium/plateauBuildingVerification.ts)
- CSS括弧対応チェック: PASS (src/App.css)
- node_modulesが同梱されていないため、フルTypeScript型チェック
  (tsc -b) およびnpm run buildは未実行。次回、依存関係を導入した環境で
  実行することを推奨。

実機確認推奨
- 手動被写体ピン配置後、案内メッセージが画面内（下部）に表示されること。
- 「138タワー」等、局所探索半径を超える構造物の検索で、被写体ピンが
  構造物の頂上付近に立つこと。
- 望遠レンズ設定・従来「確定解なし」になっていた地点・日時で、三脚候補が
  確定すること。処理時間が有意に伸びていないこと（早期収束が機能している
  こと）。

---

追記（同日）: 4件目 — 三脚候補点が「候補点計算中」のまま止まって見える

報告された実機診断（1600mm・138タワー・09:27、太陽の交点候補1件→確定0件、
roundtrip-vertical-outside-tolerance、所要時間3.6秒）を確認した結果、
計算自体は3.6秒で完了・却下されていたにもかかわらず、地図上の候補点
ラベルは「候補点計算中」の表示のまま変化していなかった。

原因: src/cesium/celestialMap.ts のupdateTripodCandidateEntities()と
src/components/Map2DOverlay.tsx の候補ラベルが、いずれも
candidate.solutionType === "preliminary" だけを見て「計算中」表示を
出していた。v20の仕様により、精密計算が「確定解なし」「通信失敗」で
終わった天体の暫定候補（solutionTypeはpreliminaryのまま）はあえて
消さずに残す設計になっているため、計算が実際には終わっているのに
ラベルだけが「計算中」で固定されたままになっていた。ユーザーからは
「三脚候補点が表示されない（＝いつまでも出てこない）」ように見える。

修正: src/cesium/celestialMap.ts, src/App.tsx, src/components/Map2DOverlay.tsx
- updateCelestialMapEntities() / updateTripodCandidateEntities() に、
  実際に計算中かどうか（tripodCandidateCalculationStatus === "calculating"）
  を新しい引数として渡すようにした。
- solutionType==="preliminary"のラベルを、計算中は従来通り「候補点計算中」、
  計算が完了（確定解なし・失敗）した後は「概算候補（確定解なし）」と表示を
  分けた。
- Map2DOverlay.tsx にも同じ区別を持つtripodCandidatesCalculatingを追加。
- 候補データ自体（位置・座標・破棄理由等）は一切変更していない。表示文言
  のみの修正。

検証
- npm install（node_modules導入）した上で以下を実行し全てPASS:
  - npx tsc -b（型チェック込みフルビルド）
  - npm run build（Vite本番ビルド）
  - npm run test:regression（33グループ、全PASS）
  - npm run lint（エラー0、警告は既存の無関係なスクリプトのみ）

---

追記（同日）: 5件目 — 実機診断で判明した「探索窓の外に真の解がある」ケース

報告された2件目の実機診断（250mm・被写体まで979m、太陽の交点候補1件→
確定0件、roundtrip-vertical-outside-tolerance、方位誤差0.0011°に対し
仰角誤差0.096°・dy=-1.745%）を分析した結果、4件目までの修正（格子解像度・
パス数の緩和）では対応しきれない、別クラスの原因を確認した。

原因: refineWithManualEquivalentProjection()の探索窓（距離方向の半径。
中心距離979mに対し radialRadiusMeters = max(24, min(80, 979*0.04)) ≈ 39m）
に対し、方位誤差はほぼゼロ（0.0011°）なのに仰角誤差だけが大きく残って
いた。これは「真に仰角が一致する距離」が、粗いECEF/DEM交点から見て
探索窓（±39m）の外側にあったことを示す。この状態で毎パス必ず探索半径を
縮める（4件目までの修正後もこの挙動は残っていた）と、外縁の点を新しい
中心として採用したまま半径だけ縮んでいき、外側にある真の解に届く前に
探索が実質的に停止し、最良候補が窓の外縁付近に張り付いたまま
0.5%の許容値内に収まらず却下されていた。

修正: src/cesium/tripodCandidates.ts refineWithManualEquivalentProjection()
- 各パスの最良候補が探索格子の外縁（距離方向・横方向いずれかの端）で
  見つかった場合は、その地点を新しい中心として同じ半径のまま次のパスへ
  進む（探索窓をスライドさせる）よう変更。半径を縮めるのは、最良候補が
  外縁ではなく窓の内側で見つかったときだけに限定した。
- 上記のスライド挙動によりパス数が増えうるため、暴走防止の上限を
  8→12へ引き上げた（1パスあたり最大81点のDEM取得はバッチ・キャッシュ
  経路のため、上限を多少上げても通信コストは限定的）。
- サンドボックス環境ではCesium 3Dシーン・実地形データを動かせないため、
  この挙動をNode.js単体の数値シミュレーションで検証した（scripts化は
  未実施、手元検証のみ）。真の解が探索窓の外（250m相当）にあるケースで、
  旧ロジック（毎パス縮小）は最終スコア7.34%（許容0.5%を大幅に超過）で
  外縁付近に張り付いて収束せず、新ロジック（外縁ヒット時はスライド）は
  8パスで0.012%まで収束することを確認した。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS（4件目までと同一の確認内容）。
- 実地形データを使う統合的な検証（実機・実際のCesium/GSI呼び出し）は
  未実施。報告いただいた2つの実機診断（138タワー・1600mm、250mm・979m）
  の場面で、今回の修正後に確定解へ到達するかどうかの再確認を推奨する。

---

追記（同日）: 6件目 — 東京スカイツリー検索でピンが根元に立つ（2件目の
再発）。2件目の修正（highestOf採用・Stage3精密化統一）だけでは、根本
原因が別にあるケースが残っていることが実機再現で判明した。

原因: resolvePlateauRoofGroundPoint()は国交省PLATEAUの「全国建物3Dデータ
セット（all-bldg-maxlod2-latest）」だけを頂上判定に使っている。この
データセットは建築基準法上の「建築物」を対象としており、東京スカイツリー
や138タワーのような鉄塔・展望塔は、通常の建物とは別区分のため、この
データセットに構造物そのものが収録されていないことがある。この場合、
探索半径・「最も高い候補を採用」等のアルゴリズム側をどれだけ改善しても、
そもそもクランプ対象の3D形状がシーンに存在しないため、原理的に頂上を
検出できない（2件目の修正はこの原因には対応していなかった）。

修正: 新規 src/height/osmSubjectHeightFallback.ts
- PLATEAU建物頂上合わせ込みが失敗した場合の追加フォールバックとして、
  OSM（OpenStreetMap）のheight / building:levelsタグから構造物・建物の
  高さを推定し、検索座標のDEM地面高からその高さぶん上空へ被写体ピンを
  配置する。
- 新しいAPI・Overpassクエリは追加せず、三脚探索のsite constraints判定
  向けに既に実装済みの仕組み（server/osmSiteContext.ts の
  nearbyStructures/nearbyBuildings、/api/osm-site-context、
  src/search/siteContext.ts の fetchSiteContexts）をそのまま流用した。
  東京スカイツリーの実際のOSMタグ（height=634, man_made=tower）で
  estimateHeightFromTags()が634m・surveyed（実測値）を正しく返すことを
  Node単体で確認済み。
- 検索座標から60m以内（PLATEAU側の局所探索20m＋広域フォールバック
  30-50mと同程度）にある、名前付きで高さ情報を持つ構造物・建物のうち
  最も近いものだけを採用し、無関係な別の構造物には反応しない。
- 高さはOSMコミュニティ入力の実測値/推定値であり、PLATEAU頂上合わせ込み
  （3d-picked、実表面）ほど確実ではないため、heightSourceに新しい値
  （osm-surveyed-height / osm-levels-estimate）を設け、由来を区別できる
  ようにした。
- PLATEAU・OSMいずれからも情報が得られない場合は、従来通りDEM地面高の
  まま変更しない（新たな0m代替・推測配置は行わない）。
- src/App.tsx resolveSearchSubject()（通常のスポット検索・被写体検索
  経路）と、標準モードのスポットプリセット適用経路の両方に同じ
  フォールバックを追加した。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- estimateHeightFromTags()の単体動作をNode.js単体で確認（東京スカイツリー
  の実際のOSMタグでheight=634m・surveyedを正しく返すこと、
  building:levelsのみの場合は3m/階の推定値になること、タグが無い場合は
  nullを返すこと）。
- 実際のOverpass API呼び出し・実機での東京スカイツリー再検索は未確認。
  ネットワーク制約上、このサンドボックスからOverpass API
  （overpass-api.de等）・GSI・Cesium ionへ到達できないため。次回、
  実機で「東京スカイツリー」「138タワー」を再検索し、ピンが構造物上方
  （東京スカイツリーなら地表+634m付近）に立つことを確認いただきたい。

---

追記（同日）: 7件目 — 東京タワー付近（都心部）の3件目の三脚探索診断で
判明した、これまでと異なる失敗パターンへの診断強化

報告された3件目の実機診断（401mm・距離217m・東京タワー付近、
roundtrip-horizontal-outside-tolerance、方位誤差0.130°・仰角誤差0.025°・
dx=1.37%・dy=-0.91%）は、これまでの2件（「僅かに閾値超過」「窓の外に
真の解」）とは異なる特徴を示していた。dx・dyの両方が閾値0.5%を大きく
超えており、単純な「あと一歩」型ではない。

このケースについて:
- 必要な補正量（方位誤差0.130°を217mで換算すると横方向約0.5m相当）は
  探索窓（半径24m、5・6件目修正後は窓外でもスライドする）に対して小さく、
  6件目までの修正が有効に働いていれば通常は収束するはずの規模である。
- したがって「探索窓が足りない」「格子が粗すぎる」という既知の原因
  （5・6件目で対応済み）とは異なる、都心部特有の要因（DEMは地表面のみを
  表しビルの屋上を表現できない、建物が密集し候補地点ごとに地盤高が
  不連続に変化する等）による可能性がある。ただしサンドボックスでは実際の
  Cesium/GSI呼び出しができず、この仮説を数値的に確認・特定できなかった。

このため、次回以降の実機診断で原因を切り分けられるよう、診断コピーの
出力を拡張した（動作・判定条件・精度は一切変更していない、診断専用の
追記）。

修正: src/cesium/tripodCandidates.ts, src/App.tsx
- refineWithManualEquivalentProjection()が実際に何パス実行したか
  （refinementPassesUsed）と、1パス目時点のスコア（firstPassScorePercent、
  dx/dyのhypot・%）・最終スコア（finalScorePercent）を記録するようにした。
- 「この検索の診断情報をコピー」の出力に、最終判定詳細の各候補ごとへ
  「精密化パス」「1パス目スコア」「最終スコア」を追加した。
- これにより次回以降の診断コピーで、(a)パス数が少ない・1パス目から
  スコアが改善していない→探索が真の解の方向へ向かえていない
  （新しい種類のバグの可能性）、(b) パス数を使い切りスコアが緩やかに
  改善しているが0.5%に届かない→都心部の候補地点密度・DEM解像度限界に
  よる真の限界、のどちらに近いかを区別できる。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- 診断専用のフィールド追加であり、候補の採否・座標・高さ計算には
  一切影響しない（reject()呼び出しへの引数追加のみ）。

---

追記（同日）: 8件目 — 東京タワーで再び根元にピンが立つ（6件目の
OSMフォールバック修正が実際には発動していなかった）

6件目の修正後、東京スカイツリーでは正しく塔の上にピンが立つことを
実機で確認いただいたが、続いて東京タワーで検索したところ、再び根元付近
にピンが立つ不具合が実機で再現した。

原因: resolveSearchSubject()（および標準モードのスポットプリセット適用
経路）が、PLATEAU屋根合わせ込み(resolvePlateauRoofGroundPoint)の結果が
非nullであれば、OSM高さ推定(findOsmSubjectHeightHint)を一度も呼ばずに
即座にそれを採用していた（`if (roofPoint) return roofPoint;`）。

東京タワーの真下には「フットタウン」という別の低層建物（土産物店・
水族館等が入る、塔本体よりずっと低い建物）が隣接しており、これは
通常の建築物としてPLATEAUの全国建物データセットに収録されている。
このため、探索座標（東京タワーの代表点）の近傍でPLATEAU側の合わせ込みが
「失敗せず」、フットタウンの屋根（塔本体よりずっと低い高さ）を「正常な
結果」として返してしまう。roofPointが非nullである以上、6件目で追加した
OSM高さ推定フォールバックには一度も到達せず、結果として塔の根元付近
（正確には隣接する低い建物の屋上）にピンが立っていた。

修正: src/App.tsx resolveSearchSubject()、標準モードのスポットプリセット
適用経路
- PLATEAU屋根合わせ込みとOSM高さ推定を、どちらも必ず並行して取得する
  よう変更（早期returnを廃止）。
- 両方が得られた場合は、最終的な標高（ellipsoidalHeightMeters）がより
  高い方を採用する。低い方を機械的に信頼せず、「より構造物本体を捉えて
  いる可能性が高い」高い方を優先することで、隣接する低い建物に引っ張られ
  なくなる。
- どちらも得られない場合は、従来通りDEM地面高のまま変更しない。

追加修正: src/height/osmSubjectHeightFallback.ts
- OSM高さ推定自体も、同様の理由で近傍の低い建物（nearbyBuildings）を
  誤って選ぶ可能性があったため、閾値内にman_made=tower等の構造物
  （nearbyStructures）が1件でもあれば、建物候補より構造物候補を優先する
  よう変更した（構造物区分はこの機能が主対象とする塔・展望塔である
  可能性が高いため）。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- 実際のOverpass API・PLATEAU呼び出しを伴う実機検証は未実施（前回までと
  同じネットワーク制約）。次回、実機で「東京タワー」を再検索し、ピンが
  塔本体上方（地表+333m付近）に立つことを確認いただきたい。あわせて、
  6件目で確認済みの東京スカイツリーが引き続き正しく表示されることも
  確認いただけると、今回の「両方取得して高い方を採用」への変更が
  スカイツリー側の挙動を壊していないことの確認になる。

---

追記（同日）: 9件目 — Googleマップ共有リンクの解決が「429」で失敗する
（スポット検索画面）

報告: スポット検索画面でGoogleマップ共有リンク（maps.app.goo.gl）を
貼り付けて検索すると、「Googleマップ共有URLを確認できませんでした。
（詳細: Googleマップ共有リンク通信エラー：429）」で失敗する。

原因: server/googleMaps.ts には、2026-08-25追記のGoogle側429（レート
制限）対策として、429応答時に1.5秒待って1回だけ再試行する仕組みが
既に入っていた。しかしCloudflareのエッジは他の利用者・他サイトとIPを
共有するため、混雑時はこの1回・1.5秒の再試行だけでは制限が解消せず、
再試行後も429が返り続けて失敗していた。

修正: server/googleMaps.ts withRateLimitRetry()
- 再試行を1回→最大3回へ増やし、待機時間を指数バックオフ
  （1.5秒→3秒→6秒、最大6秒で頭打ち）に変更した。
- 複数の再試行が同時に同じタイミングでGoogleへ再アクセスして429を
  再発させる（thundering herd）のを避けるため、各待機時間に±20%の
  乱数ジッターを加えた。
- 429以外のHTTPエラー（無効なURL・404等）は従来どおり即座に失敗として
  扱う（対象を429だけに限定する方針は維持）。
- resolveGoogleMapsSharedUrl()内の全てのfetch呼び出し（リダイレクト追跡・
  最終ページ取得・Google Places API問い合わせ）は元から同一のfetcherを
  共有しているため、この修正だけで経路全体に反映される。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- 再試行ロジック自体をNode.js単体でモックfetcherに対してシミュレーション
  し、(a) 2回連続429の後に成功するケース（旧: 1回再試行のみのため失敗
  していたはずのケース）が新ロジックでは成功すること、(b) 3回連続429の
  後に成功するケース（新ロジックの再試行上限ぎりぎり）も成功すること、
  (c) 5回連続429など再試行上限を超えて持続する障害は、無限ループせず
  429のまま呼び出し元へ返ること、をそれぞれ確認した。
- 実際のGoogleマップ・Cloudflareエッジを介した実機検証は未実施
  （ネットワーク制約により、このサンドボックスからGoogleマップの共有
  リンク解決エンドポイントへ到達できないため）。次回、混雑時間帯に
  同じ共有リンクで再試行し、429が解消されるか確認いただきたい。

---

追記（同日）: 10件目 —「そもそも再試行が必要になるのがおかしい。失敗
しない仕様にしてほしい」というご指摘への対応

ご指摘の通り、9件目の対応（再試行回数を増やす）は対症療法であり、
「Googleへ問い合わせるたびに毎回レート制限のリスクにさらされる」という
根本の設計は変わっていなかった。今回は、そもそもGoogleへ問い合わせる
回数自体を減らす方向で対応した。

正直な前提の確認: Googleマップの共有URL解決は、外部（Google）のサーバー
へ実際に到達して初めて座標が分かる性質上、「絶対に失敗しない」保証は
原理的に作れない（Google側のレート制限は当方が制御できない外部要因の
ため）。目指せるのは「同じURLについて、一度成功すればその後は二度と
Googleへ問い合わせない（＝二度と失敗しうる状態に戻らない）」という設計
であり、今回はこれを実装した。

修正1: functions/api/resolve-google-maps.ts
- Googleマップ共有URLの解決結果を、/api/geocodeと同じR2永久キャッシュ
  （functions/_shared/r2Cache.ts の getOrCreateR2Json、30日保持）へ
  保存するようにした。地図上の地点そのものは動かないため、一度解決
  できた座標は長期間有効である。
- 同じ共有URLの2回目以降のリクエスト（同じ利用者の再試行だけでなく、
  別の利用者が同じ共有リンクを開いた場合も含む）は、Googleへ一切
  アクセスせずキャッシュから即答するようになった。これにより、
  Googleへの実際の問い合わせ回数そのものが減り、429が発生する機会が
  構造的に減る。
- 同時に複数の同一URLリクエストが飛んだ場合の重複問い合わせ抑制
  （in-flight共有）も、既存のgetOrCreateR2Jsonの仕組みにより自動的に
  効く。
- 解決に失敗した場合（例外）はキャッシュへ保存されないため、失敗結果を
  誤って使い回すことはない。

修正2: src/search/nativeGoogleMapsResolver.ts（ネイティブ版アプリ経路）
- サーバー経路と同じ指数バックオフ再試行（最大3回、1.5秒→3秒→6秒＋
  ジッター）を追加した。ネイティブ経路は端末自身のIPで通信するため
  共有IP起因の429は起きにくいが、念のため同じ考え方で備えた。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- scripts/verify-google-maps-url.mjs（既存の回帰テスト）も実行し直し、
  「Native Google Maps resolver test skipped: dependencies are not
  installed.」という表示が今回の変更前から存在する、無関係な既知の制約
  （このスクリプトをVite経由でなく素のNode ESMで直接実行すると、拡張子
  省略の相対importが解決できないためスキップされる。実際のアプリは
  Vite経由で動くため影響しない）であることを、変更前のコードでも同じ
  スキップが起きることを確認した上で切り分けた。
- R2キャッシュ・in-flight共有の仕組み自体（getOrCreateR2Json）は
  /api/geocodeで既に本番運用されている既存コードをそのまま流用して
  おり、新規のキャッシュ実装は行っていない。
- 実際のGoogle・Cloudflareエッジ・R2を介した実機検証は未実施
  （ネットワーク制約）。次回、同じ共有リンクを2回連続で開き、1回目に
  成功していれば2回目が明らかに速く返る（キャッシュ由来）ことで、
  この修正が効いているか確認いただきたい。








追記（同日）: 11件目 —「確定した三脚候補」の精度そのものが誤っていた
（現地確認により判明）

報告: 2026/08/09 18:06・250mmで再検索したところ「確定した三脚候補」と
表示され、地形取得0.2秒という異例の速さで確定した。しかし実際にこの
日・この場所へ行って確認したところ、正しい三脚位置は表示された候補
（約1252m先）ではなく、別の地点（測定968m）だった。

これは5・6・7件目の「僅かに閾値超過」「窓の外」型の問題とは異なり、
アルゴリズムが実在する誤った交点を"確定"として返してしまう、より深刻な
種類の不具合だった。「確定」表示は本来ユーザーが安心して現地へ向かえる
根拠になるべきものであり、誤った確定は「確定解なし」より実害が大きい。

原因: src/cesium/tripodCandidates.ts の一次探索（scanInitialRayTerrain
Intersections）は、高速化のため探索範囲の上限（primaryMax）と対数サンプル
密度を「距離ヒント」（searchProfile.preferredDistanceMeters。被写体・日付が
同じ場合にApp.tsx側でキャッシュから渡される「前回の確かな答え」）を基準に
決めていた。しかし被写体が同じでも日時が変わると天体の方位・仰角は変わる
ため、キャッシュされた時点では正しかった距離が、今回の検索では地形上の
別の（誤った）交点に近い値になっていることがある。この状態で探索範囲の
上限まで変えてしまうと、対数サンプルの密度も変わり、真の交点（968m付近）
を取りこぼす一方、ヒントに近い別の交点（1252m付近）だけを見つけて
しまうことがある。この別の交点もround-trip判定を通ってしまえば「確定」と
表示されるため、想定していた安全弁（ヒントが外れた場合は全距離走査へ
自動フォールバックする）は働かない（安全弁は「探索範囲内で何も見つから
なかった場合」だけに効き、「探索範囲内で別の交点が見つかってしまった
場合」には効かないため）。

修正: src/cesium/tripodCandidates.ts scanInitialRayTerrainIntersections()
- 探索範囲の上限・サンプル密度を決める値（rangeSizingDistanceMeters）を、
  常にその場で新しく計算される幾何学的estimate（directSeedDistance、
  天体の現在の方位・仰角から求まる、日時に依存しない意味で常に最新）
  だけを使うよう変更した。距離ヒント（searchProfile.preferredDistance
  Meters）はもう探索範囲・サンプル密度には一切影響しない。
- 距離ヒントは、探索へ追加で注入する1点（injectedSampleDistanceMeters）
  としてのみ使い続ける。これは既存の対数サンプル密度・範囲を削ったり
  動かしたりしない「追加の1点」でしかないため、ヒントが正しければ精密化を
  速められる利点は保ったまま、ヒントが外れていても他の真の交点を
  取りこぼすリスクを排除した。
- directSeedDistanceが得られない場合（celestial bodyの幾何が不定な場合等）
  は、以前の「距離ヒントだけで範囲を決める」動作ではなく、より安全な
  全距離走査（exhaustive）へ回るよう変更した。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- 探索範囲・精密化ロジック・round-trip判定条件（0.5%）・DEM解像度は
  変更していない。変更したのは「一次探索の範囲をどの値で決めるか」
  だけであり、preferredDistanceMetersはこれまで通り追加サンプルとしては
  使われ続ける。
- 実際のCesium/GSI呼び出しを伴う実機検証は未実施（ネットワーク制約）。
  次回、同じ日時・地点（2026/08/09 18:06・250mm）で再検索し、確定候補が
  現地確認済みの正しい距離（約968m）付近になるか確認いただきたい。

追記（同日）: 12件目 — 再検索時に「manual-refinement-exception」・
所要時間15秒という、通信内訳と矛盾する診断が出た

報告: プロジェクト読込後に改めて「検索」を実行したところ、「確定解
なし」（概算候補1368m）となり、診断コピーに「manual-refinement-
exception」「所要時間15.1秒」と出た。しかし内訳の合計（初期探索0.03秒・
気象0.00秒・収束反復0.05秒・精密化0.00秒・ダブルチェック0.00秒＝
0.08秒）とは全く整合しない長さで、通信回数も15回・合計0.1秒と少なく、
何が15秒かかったのか説明がつかない状態だった。

原因: src/cesium/tripodCandidates.ts の
refineWithManualEquivalentProjection()呼び出しを囲うtry/catchが、
どんな例外も一律に「manual-refinement-exception」として診断へ記録して
いた。しかしこの中には、新しい検索が開始されて古い検索が中止された
場合に発生する正常な中止（AbortError）も区別せず含まれていた。中止は
本当の失敗ではなく、"古い検索がキャンセルされて新しい検索に置き換わった"
だけの正常動作である。これを失敗として記録・表示してしまうと、実際には
別の（新しい）検索の結果が出ているにもかかわらず、画面には中止された
古い検索の「確定解なし」という診断が残ってしまう。今回の「15.1秒」
「通信内訳と矛盾する」という不自然な数値は、中止されるまでの経過時間が
そのまま記録されていたためだと判明した（このコードベースには元々
isAbortError()で中止を区別してPromise.allSettledへ伝播させる既存の
仕組みがあり、他の箇所では使われていたが、この1箇所だけ抜けていた）。

修正: src/cesium/tripodCandidates.ts
- refineWithManualEquivalentProjection()呼び出しのtry/catchで、
  isAbortError(error)がtrueの場合はreject()を呼ばず、診断へも記録せず、
  そのままthrowして呼び出し元（Promise.allSettledのisAbortError判定、
  既存の仕組み）へ伝播させるよう変更した。中止以外の本当の例外は
  従来どおりreject("manual-refinement-exception", ...)で記録する。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- 中止判定（isAbortError）・Promise.allSettledでの伝播先は、この
  コードベースの既存パターン（例: processInitialSolutionの呼び出し元、
  1763行目付近）をそのまま踏襲しており、新しい仕組みは導入していない。
- この修正は「なぜ中止されたか」（項目11の再検索と、プロジェクト読込時に
  走る可能性のある別の検索が競合した可能性等）そのものは変えておらず、
  「中止を失敗として誤って記録・表示しない」ようにするものである。次回、
  同じ操作（プロジェクト読込→検索）を行って再現するか、また項目11の
  修正が実際に反映された新しい確定候補（968m付近を期待）が得られるかを
  確認いただきたい。

追記（同日）: 13件目 —「manual-refinement-exception」・所要時間
約15秒が、v21再ビルド後も再現し続けた（12件目の中止判定修正だけでは
直らなかった）

12件目でAbortError（正常な中止）を誤って失敗記録していた問題を修正した
が、ビルド・デプロイ後に全く同じ「manual-refinement-exception」
「15.1〜15.2秒」が再現し続けることが確認された。これは中止
（AbortError）ではなく、実際には別の、本当の例外だったことを意味する。

原因を特定: src/cesium/tripodCandidates.ts の
buildPointSpecificFinalCandidateGroundPoint()が、精密化の最後に候補点
1点だけの高精度ジオイド高を国土地理院ジオイドCGI
（fetchGsiGeoidHeightPointSpecific → /api/gsi-geoid）へ問い合わせて
おり、これを無条件にawaitしていた。この関数のタイムアウト
（GEOID_FETCH_TIMEOUT_MS）はちょうど15000ミリ秒（15秒）に設定されて
おり、実機診断の「15.1〜15.2秒」という毎回ほぼ同じ長さは、このタイム
アウトが実際に発火していたことを示す（コード自体のコメントにも
「国土地理院ジオイドCGIは応答が不安定」と明記されている、既知の外部
API不安定性）。このタイムアウトはAbortSignal.timeout()由来のため
name="TimeoutError"になり、isAbortError()（name==="AbortError"だけを
見る）には該当しないため、12件目の修正では区別されず、そのまま
「manual-refinement-exception」として候補全体が棄却されていた。

このジオイドAPI呼び出しは、候補点1点だけの「センチメートル級」の高精度
値を得るためのものだが、探索中に既に取得できている地域代表値
（0.01度単位、ミリ未満の精度差しかない滑らかな量）で十分実用に足りる
場面がほとんどであり、これが失敗・タイムアウトしただけで候補全体を
失うのは過剰な代償だった。

修正: src/cesium/tripodCandidates.ts
buildPointSpecificFinalCandidateGroundPoint()
- 候補点別の高精度ジオイド高取得を、無条件の待機からベストエフォート
  （try/catch）へ変更した。
- 取得できればそれを使用（従来どおりの精度）。
- 取得に失敗・タイムアウトしても、探索中に既に得られている地域代表値
  （sampledGeoid）が使える場合は、それを代わりに使って候補を成立させる
  （ミリ未満の精度差のみ、候補の採否・座標は変わらない）。
- 中止（AbortError）は従来どおり即座に伝播させ、候補を握りつぶさない。
- 地域代表値も無い場合（両方失敗）だけ、従来どおり例外を投げて候補を
  棄却する。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- round-trip判定条件・探索範囲・DEM解像度・11件目の距離ヒント分離修正は
  変更していない。この修正は「候補点別の高精度ジオイド値が取れない場合
  だけの、僅かな精度低下（ミリ未満）を伴うベストエフォート化」であり、
  精度を落とす代わりに候補を失わないようにするトレードオフである。
- 実際のGSIジオイドCGI・タイムアウト挙動を伴う実機検証は未実施
  （ネットワーク制約）。次回、同じ操作（プロジェクト読込→検索）で
  15秒待たされることなく、11件目の修正どおり968m付近の確定候補が
  得られるか確認いただきたい。

追記（同日）: 14件目 —「確認せず確定させるな」というご指摘への対応：
推測での修正を止め、原因特定用の生データ診断を追加

ご指摘: 11件目の修正（距離ヒントとレンジサイズの分離）後も再検索したが、
結果は変わらず1285m付近のまま確定していた（現地確認は968m）。ズレの
原因を確認せずに次々と推測で直そうとするのは順序がおかしい、という
指摘を受けた。この指摘は正当であり、これ以上"それらしい理論"だけで
コードを変更するのをやめ、実データで原因を特定できる手段を用意した。

現状の理解: 11件目の修正により、一次探索の範囲（primaryMax）は
「距離ヒント」ではなく「その場で新しく計算される幾何学的estimate
（directSeedDistance）」だけで決まるようになったはずだが、結果が
1285m付近のまま変わらなかった。これは次のいずれかを意味する:
(a) directSeedDistance自体が既に1285m付近を指しており、968m付近は
    そもそも探索窓に入っていない（範囲外）
(b) 968m付近は探索窓に入っているが、離散サンプリングの粗さにより
    その付近での符号変化（＝交点の兆候）が検出されていない
(c) 968m付近では、そもごレイ高と地形高の差の符号が変化しておらず
    （＝この計算モデルの上では968mは"交点"ではない）、1285m付近が
    数学的には正しい解になっている
これらは見た目の症状（「1285mのまま」）からは区別できず、推測で
コードを変更しても中るとは限らない。したがって、今回はコード変更では
なく診断を追加した。

追加: src/cesium/tripodCandidates.ts, src/App.tsx
- scanRayTerrainIntersections()の粗探索段階（密度補完後・精密化前）で
  実際に計算された「距離ごとのレイ高と地形高の差[m]」の生データを、
  直近1回分だけ保持するようにした（getLastCoarseScanSamples()）。
- 「この検索の診断情報をコピー」に、この生データ（距離昇順、
  "距離m:差m"の空白区切り）をそのまま追記した。符号が変わっている
  （負→正、または正→負）箇所が"交点の兆候"であり、968m付近にそれが
  あるかどうかを目視で直接確認できる。
- 探索アルゴリズム・判定条件・座標計算は一切変更していない。100%
  診断専用の追記であり、動作・精度・確定/棄却の結果には影響しない。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。

次回のお願い
- 同じ操作（プロジェクト読込→検索）を行い、「この検索の診断情報を
  コピー」の内容全体（末尾の生データ行を含む）を貼っていただきたい。
  この生データを見れば、968m付近に符号変化があるのに見落とされて
  いるのか、それとも本当に無いのかが直接判明し、次にどこを直すべきか
  （探索範囲の問題か、サンプリング密度の問題か、そもそも計算モデルの
  前提が違うのか）を推測ではなく確定できる。

追記（同日）: 15件目 —「以前は正確だった。いつ計算方式を変えたのか」
というご指摘への調査結果：計算方式ではなく、永続キャッシュの自己強化
バグが原因だった

ご指摘を受けて、tripod候補計算に関わる変更履歴（IMPLEMENTATION_TRIPOD_
SPEED_SAFE_V11〜）を遡って確認した。

判明した事実: v11（IMPLEMENTATION_TRIPOD_SPEED_SAFE_V11）で、新規
src/cesium/tripodCandidateSeedCache.ts が導入されていた。これは
「確定済み（aligned）候補の距離・座標を、端末のIndexedDBへ最大30日間
保存し、次回以降の同じ被写体・天体・方位帯（0.5°単位）・高度帯
（0.5°単位）の検索でヒントとして再利用する」永続キャッシュである
（日付・時刻そのものは区別せず、太陽の見かけの方位・高度が近ければ
同じキャッシュを共有する設計）。

これ自体は「ヒントが外れても既存の全距離探索へフォールバックするので
安全」という前提で設計されていたが、実際には次の自己強化ループが
起こり得ることが分かった:
1. 何らかの理由で誤った候補が「確定（aligned）」と判定される
   （本レポートの一連のやり取りで実際に複数回発生した）
2. savePersistentTripodSeeds()が、確定済みという理由だけでこの誤った
   距離を無条件に端末へ永続保存する
3. 以後、同じ被写体・天体・方位帯・高度帯に該当する検索は毎回この
   誤った値をヒントとして受け取り、そこに引っ張られて同じ誤った答えを
   再確認しやすくなる
4. 再確認された誤った答えが再度保存され、誤りが強化され続ける
この永続データは端末のIndexedDBに保存されるため、アプリのコードだけを
新しくしても（本レポートで11〜14件目の修正を配布・デプロイ済みでも）
消えない。「以前は正確だった」のは、この永続キャッシュが導入される
前、またはまだ誤った値で汚染される前の時点だったためであり、計算方式
（アルゴリズム）そのものが劣化したわけではないと考えられる。

修正: src/cache/deviceCache.ts, src/cesium/tripodCandidateSeedCache.ts,
src/App.tsx
- deviceCache.tsに、名前空間単位で永続データを即座に全削除する
  clearDeviceCacheNamespace()を追加した（既存のnamespaceインデックスを
  利用、TTL経過を待たない）。
- tripodCandidateSeedCache.tsのSEED_VERSIONをv1→v2へ更新した。
  キーにバージョン文字列を含む設計のため、これだけで今回の更新以前に
  保存された（汚染された可能性のある）seedは全て自動的に無効化される
  （明示的な削除処理は不要）。
- 万一、更新後に同じ問題が別の形で再発した場合に、利用者自身の操作で
  即座にリセットできるよう、clearPersistentTripodSeeds()と、三脚探索の
  診断コピー欄に「三脚候補の記憶をリセット」ボタンを追加した。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ、scripts/verify-tripod-speed-cache-20260829.mjsを含む）
  / npm run lint、全てPASS。
- 探索アルゴリズム・round-trip判定条件・DEM解像度・11〜13件目の修正は
  変更していない。今回の変更は「誤って永続化されたseedを無効化する」
  ことと「再発時に利用者が自分でリセットできるようにする」ことの2点。

次回のお願い
- 今回のビルドを適用した時点で、汚染されていた可能性のある永続seedは
  自動的に無効化されているはずである。念のため、検索前に「三脚候補の
  記憶をリセット」ボタンを押してから、改めて同じ地点・日時で検索し、
  968m付近の確定候補が得られるか確認いただきたい。
- もし依然として1252m/1285m付近で確定する場合は、それは永続キャッシュ
  の問題ではなく、14件目で追加した粗探索の生データ診断（距離昇順の
  "距離:誤差[m]"列）で、968m付近に本当に符号変化があるかどうかを直接
  確認する段階に進む。

追記（同日）: 16件目 —「仕様が目的と違っている可能性」というご指摘への
対応：round-trip投影条件だけでは検出できない、視線遮蔽の見落とし

これまでの調査で判明した2つの事実を踏まえ、根本的な設計面を再検証した:
(a) 今回のプロジェクト「実証」は、2026-08-23付のCLAUDE_HANDOFF文書で
    既に一度扱われていた既知の回帰テストケースだった（「現地確認した
    正しい三脚位置と、自動算出候補が別地点に出る」という同一症状）。
(b) 2026-08-23仕様は「ECEF 3Dレイと地形の交点」「round-trip投影一致」を
    確定条件として定義しており、現在のコードもこれに忠実だった。しかし
    仕様そのものに、「候補地点から実際に被写体が見えるか（途中の地形に
    遮られていないか）」という検証条件が含まれていなかった。

これが「仕様が目的と違っている可能性」の具体的な中身である。目的は
「実際に立って撮影できる場所を教えること」だが、仕様が定義する確定条件
（幾何学的にレイ上にあり、round-trip投影が一致すること）は、途中に
別の地形が視界を遮っていても満たされてしまう。

具体的な機序: 被写体・天体方位が同じレイは、地形（例: 川の堤防）が
起伏していれば複数回地表と交差しうる。近い交点（堤防の被写体側斜面）
からは被写体への視線が通るが、遠い交点（堤防の反対側斜面）は、その
堤防自体に視線を遮られ、実際には被写体が見えない。round-trip投影条件
だけでは、ある点が幾何学的にレイ上にあれば、途中に何があろうと満たして
しまうため、この2つを区別できない。これは「現地確認済みの近い交点では
なく、より遠い交点が誤って確定表示される」という一連の報告と整合する。

対応: src/cesium/tripodCandidates.ts, src/types/celestial.ts,
src/cesium/celestialMap.ts, src/components/Map2DOverlay.tsx
- 候補地点（カメラ高込み）から被写体までの直線上に、途中の地形が実際に
  飛び出していないかを、候補・被写体間を9点サンプリングして確認する
  candidateSubjectLineOfSightClear()を追加した。
- 重要: 最初の実装では、視線が遮られている疑いのある候補を除外する形に
  したところ、既存の回帰テスト（tests/regression/tripod-candidate-
  round-trip.test.mjs、「2つの交点はどちらも候補として保持されるべき」）
  に失敗した。これは2026-08-23仕様「D. 地形との複数交点：レイが地形と
  複数回交差する場合、全交点を候補として保持する。遠い候補を勝手に1点へ
  絞らない」に反する変更だったため、除外ではなく注釈（フラグ付与）方式へ
  修正した。
- TripodCandidateに`lineOfSightPossiblyObstructed`（視線遮蔽の疑い）と
  `obstructionDistanceMeters`（遮っている疑いのある地形までの距離）を
  追加。このフラグが立っていても候補からは除外しない。
- 地図上の候補ラベル（3D: celestialMap.ts、2D: Map2DOverlay.tsx）に
  「⚠視界不良の可能性」の注意書きを追加し、ユーザー自身が最終判断できる
  ようにした（DEMデータの誤差や、実際には橋・切通し等で視界が通っている
  可能性もゼロではないため、機械的な除外ではなく注意喚起にとどめる）。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ、tripod-candidate-round-trip.test.mjsの8件全てPASSを
  含む）/ npm run lint、全てPASS。
- 除外ではなく注釈のみの変更のため、候補の座標・件数・確定/棄却の判定
  そのものには一切影響しない。
- 実際の地形データでこのフラグが今回のケース（1252m/1285m付近の候補）
  で実際に立つかどうかは、サンドボックスから実データへアクセスできない
  ため未確認。次回の実機検証で、遠い方の候補に「⚠視界不良の可能性」が
  表示されるか確認いただきたい。

追記（同日）: 17件目 — V11〜V20のうち「仕様準拠か否かではなく、純粋に
精度に関わる変更」を全てピックアップし、1件ずつ検査した結果

ご指示に従い、V11〜V20の実装メモ11本を全て読み直し、「精度に影響しうる
変更」に該当するものを漏れなく抽出し、1件ずつ現在のコードと突き合わせて
検査した。結果は以下の通り（見つかった全件）。

【V11】
1. 永続seedキャッシュ導入（searchProfile.preferredDistanceMetersの新設） 
   → 精度に影響しうる変更。文書は「wide fallbackが必ず残るため最終精度
   条件は不変」と主張していたが、この主張は誤りだったことを既に確認
   済み（6・11・15件目で対応：wide fallbackは「0件」時にしか働かず、
   「別の誤った交点が見つかってしまう」場合には働かない）。
2〜4. DEMタイルIndexedDB読み出しの共有・並列化
   → 純粋なI/Oスケジューリング変更。値の読み出し・優先順位・補間式は
   変更されておらず、コード確認の結果、精度への影響は見当たらない。
5. 完全一致結果キャッシュ（セッションメモリのみ）
   → 精度に影響しうる変更。今回新たに発見: このキャッシュのキー
   （exactTripodCacheKey、重複探索抑止のキーとしても共用）に、
   searchProfile.preferredDistanceMetersに相当する値
   （preferredDistancesById）が含まれていなかった。同じ被写体・日時・
   カメラ設定でも、距離ヒントが異なれば異なる交点が確定しうることは
   本レポートの一連の調査で確認済みのため、これはキャッシュ完全性の
   欠落だった（ヒントだけが変わった再探索を「同一」と誤認し、別ヒント
   下での結果を誤って再利用しうる）。今回修正。

【V12・V13】
DeviceCacheのバッチ読み書き、DEMタイルのバッチ・並行IndexedDB読み出し、
seed DEMウォームアップの待機順序変更。
→ いずれも純粋なI/Oスケジューリング変更。優先順位・値・キー・TTLは
「変更しない」と明記され、コード確認でもその通りだった。精度への影響は
見当たらない。

【V14】
三脚候補計算の重複起動抑止（同一入力なら2回目を無視）。
→ 精度に影響しうる変更（抑止キーが不完全なら、異なる入力の探索が
誤って「同一」とマージされうる）。抑止キーはV11のexactTripodCacheKeyと
共用であるため、上記5.と同じ欠落を共有していた。今回の修正で同時に解消。

【V15】
工程別所要時間の計測（performance.now()の追加のみ）。
→ 計算式・分岐条件を一切変更していないことをコードで確認。精度への
影響なし。

【V18】
「確定0件」時の棄却理由を診断へ記録する追加のみ。
→ reject()呼び出しの追加であり、候補の採否ロジック自体は変更して
いないことをコードで確認。精度への影響なし。

【V19】重要
1次元（距離のみ）だった手動ピン相当の最終精密化を、2次元（距離+方位）
探索へ置き換えた変更。
→ これは純粋な高速化ではなく、三脚候補の最終座標を決めるアルゴリズム
そのものの変更（本レポートでも5・6・11件目で直接手を入れている核心
部分）。文書自身が「A real-device run at the failing scene is still
required to prove that this was the only cause」（実機での検証が
まだ必要）と明記しており、当時から未検証のまま出荷されていた。
今回、round-trip検証（verifyRoundTripProjection）・評価関数
（evaluateManualEquivalentCandidate）を再検査し、天体方位・高度が
候補ごとに正しく再計算されていること、viewCorrectionが物理座標に
混入していないこと等、コードの内部整合性は確認できたが、実データに
よる最終検証はサンドボックスの制約上できていない。

【V20】
1. 中断後再開不具合の修正（runId導入）→ ロジック変更だが、目的は
   「中断されたら解放する」ことで、探索アルゴリズム自体は不変。
2. 気象等I/O待ち前の概算候補の即時表示 → 表示のみ、確定候補には無関係
   （preliminaryはalignedを置換しない設計を確認）。
3. 天体ごとの完了順表示 → 表示順のみ、最終値は不変（コードのコメント
   「最終戻り値の候補値・候補順・採否条件は変更しない」を確認）。
4. 失敗時の概算候補維持 → 表示のみ。ただしこれが8・9件目で修正した
   「候補点計算中のまま止まって見える」表示バグの原因だった。
5. キャッシュ準備2秒打ち切り → キャッシュはseedとしてのみ使われ、
   打ち切られても通常探索（authoritative経路）へ進むことをコードで確認。
6. World Terrain 30秒タイムアウト・最大3回再試行 → タイムアウト時は
   例外を投げるのみで、低解像度データへの置換は無いことをコードで確認。

結論
- V11〜V20のうち、実際に最終候補の座標・確定可否を左右しうる「純粋な
  精度関連の変更」は【V11-1】【V11-5/V14共用】【V19】の3件（重複込み）。
- このうち【V11-1】【V11-5/V14】は今回修正済み（6・11・15・17件目）。
- 【V19】（1D→2D精密化アルゴリズムそのもの）は、コード内部の整合性は
  確認できたが、文書自身が認める通り実機検証が最初から行われておらず、
  今回のサンドボックスでも実地形データによる検証はできない。現時点で
  疑わしい具体的な不整合は見つかっていないが、「未検証のまま出荷された
  核心アルゴリズム」であることは正直に記録しておく。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。

追記（同日）: 18件目 —「三脚候補の記憶をリセット」ボタンが
「診断情報をコピー」ボタンと完全に重なり、コピーできない

原因: 15件目で「三脚候補の記憶をリセット」ボタンを追加した際、既存の
「この検索の診断情報をコピー」ボタンと同じCSSクラス
（.map-tripod-diagnostics-copy）をそのまま使い回した。このクラスには
position: absolute; top: 78px; left: 50%; transform: translateX(-50%)
という「画面上の絶対位置」が指定されていたため、2つのボタンが完全に
同じ座標へ重なって描画されてしまい、後から描画される方（リセット
ボタン）が常に手前になって、コピー ボタンをタップできなくなっていた。

修正: src/App.tsx, src/App.css
- 2つのボタンを新しいラッパー要素（.map-tripod-diagnostics-actions、
  display:flex・横並び・折り返し対応）で囲み、位置指定はこのラッパー
  側だけに持たせた。
- .map-tripod-diagnostics-copy自体からposition/top/left/transformを
  外し、ラッパー内で横に並ぶ通常のボタンへ変更した。
- 画面幅が狭い場合はflex-wrapで自動的に折り返す。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。CSSの括弧対応も確認済み。

なお、ご報告いただいた「三脚候補点の表示もない」点について: 今回の
「記憶をリセット」後の検索では、地形未確認の理論値（太陽概算候補
1404m）のみが表示され、実地形に基づく確定/未確定いずれの候補点も
出ていないように見える。これがリセット直後特有の一時的な状態
（探索がまだ完走していない）なのか、別の不具合なのかは、今回のボタン
修正で診断コピーが取得できるようになった後、内容を確認して切り分ける。

追記（同日）: 19件目 —「V11〜V20の変更を見直せば分かるだろ」というご指摘
への対応：「記憶をリセット」が不完全で、かつ新しい検索を自動実行して
いなかった

V11〜V20の変更内容（特にV11の永続seedキャッシュ設計）を見直した結果、
2つの不備が見つかった。

不備1: リセットが永続層だけで、優先度がより高いセッション内の記憶を
消していなかった
V11の実装メモは明記している：「現セッションの直前確定候補が存在する
場合は、そちらを永続seedより優先」。実際のコード（App.tsx）でも、
tripodCandidatesRef.current（前回の確定候補。誤った候補が確定して
しまった場合はその値も含む）から作るpreferredDistancesByIdが、永続
seedより優先して使われる設計になっている。18件目までの「記憶を
リセット」ボタンはtripodCandidateSeedCache.ts（永続層、IndexedDB）
だけを消去しており、この優先度がより高いセッション内の記憶
（tripodCandidatesRef.current）は消していなかった。そのため、
リセットを押しても、直前に確定してしまった誤った候補がそのまま
優先ヒントとして使われ続け、リセットが実質的に機能していなかった。

不備2: リセット後に新しい検索を自動実行していなかった
三脚候補の検索はReactのuseEffectで、被写体・日時・カメラ設定等が
変わるたびに自動実行される作りになっている。「記憶をリセット」ボタン
はこれらの依存値を何も変更しないため、リセットを押しただけでは新しい
検索が走らず、画面には「たまたま最後に完了していた（リセット前の）
検索結果」がそのまま残り続けていた。アプリには既に「検索」ボタンや
エラー通知の「再試行」から使われている手動再実行の仕組み
（tripodCandidateRetrySequenceという回数カウンタをインクリメントする
と、useEffectの依存配列に含まれているため強制的に再実行される）が
あったが、リセットボタンはこれを呼んでいなかった。

修正: src/App.tsx handleResetTripodSeedCache()
- 永続seed（clearPersistentTripodSeeds）に加えて、セッション内の記憶
  （tripodCandidatesRef.current、tripodHintSubjectRef.current）も
  同時にクリアするようにした。
- 既存の手動再実行の仕組み（setTripodCandidateRetrySequence）を呼び、
  リセット直後に新しい検索を自動的に開始するようにした。
- これにより「リセットを押す」→「セッション・永続の両方の記憶が消え、
  かつ即座に新しい検索が走り、その結果が画面に反映される」という、
  ボタンの名前が示す通りの動作になる。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- リセット後に自動的に走る検索自体のアルゴリズム（探索範囲・精密化・
  round-trip判定等）は一切変更していない。

追記（同日）: 20件目 —「プレビュー画面と三脚候補点のアンマッチ」の
根本原因を特定：探索とプレビューでDEM補間方式が異なっていた

報告: 実際に設置している三脚ピン（968m、現地確認済み）ではプレビュー
画面が天体・被写体完全一致になるのに、探索が確定する三脚候補点は別の
場所（1252m）だった。以前も同様の症状があった。

この回の実機診断で決定的な証拠が得られた:
- 交点候補3件→確定1件（今までで最も多くの交点を検出できている）
- 現地確認済みの位置に極めて近い候補（距離979.14m）が実際に見つかって
  いたが、方位誤差はほぼゼロ（0.001122°）にもかかわらず、仰角誤差
  0.095982°・dy=-3.8669%でroundtrip-vertical-outside-toleranceにより
  棄却されていた（6パスの精密化を経てもスコアは4.22%→3.87%までしか
  改善しなかった）
- 実際に置かれている三脚ピン（968m、979.14mから僅か約11m）ではプレビュー
  が完全一致するのに、探索内部では同じような地点で無視できない誤差が
  残り続ける、という矛盾。

原因: 三脚候補探索（粗探索・精密化とも）は、位置探索の精度を優先して
DEM高さをneutral補間（sampleWorldTerrainNeutral、制約付きBicubic、
LOS用の上方バイアス無し）で取得している。一方、実際に三脚ピンを置いた
とき（共有プロジェクトの取り込み時のresolveGroundPoint、通常の3Dタップ
配置等）はlos-safe補間（sampleWorldTerrainのデフォルト、
max(Bilinear, Constrained Bicubic)、上方バイアスあり）で高さを取得
している。同じ緯度経度でも、この2つの補間方式は堤防の斜面のように
局所的に地形が変化する場所では数十cm〜数mずれることがあり、望遠
（554mm）ではこの程度の高さの差だけで仰角誤差0.096°・round-trip
判定の失敗を引き起こすのに十分である。

これは2026-08-23仕様「G. Round-trip検証：候補地点を既存プレビューと
同じCameraModel/Projection経路へ逆投入する」の意図に反する。同じ投影
関数を使っていても、そこへ渡す高さの取得方法（補間方式）が実際の
プレビューと違えば、round-trip検証は「本当にそこへ三脚を置いたときの
プレビュー」を保証しない。

修正: src/cesium/tripodCandidates.ts
- 探索中の位置決定（粗探索・精密化のグリッド探索）はneutral補間のまま
  変更していない（位置を精密に絞り込むにはこちらが適切なため）。
- 精密化が完了し、最終確定判定を行う直前にだけ、実際にその座標へ三脚
  ピンを置いた場合と同じlos-safe補間（sampleWorldTerrain、標準の
  ground point解決経路と同一）で高さを取り直し、その高さでround-trip
  判定（verifyRoundTripProjection）をやり直すようにした。
- los-safe再取得に失敗した場合は、従来通りneutral補間の結果をそのまま
  使う（フェイルセーフ、候補を失わない）。
- これにより「探索が確定と言った候補」と「実際にそこへ三脚を置いた
  ときのプレビュー」が常に一致するようになる。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- 探索中の位置探索アルゴリズム自体（neutral補間によるグリッド探索、
  探索範囲、パス数等）は一切変更していない。変更したのは、最終確定
  判定の直前の高さ取得方式だけである。
- 実際のGSI DEM・実機での再検証は未実施（ネットワーク制約）。次回、
  同じ地点・日時で再検索し、979m付近（現地確認済みの968mに近い場所）
  が今度は確定するか、あるいは依然として仰角誤差が残るか（＝この
  仮説だけでは説明しきれない別要因が残っている）を確認いただきたい。

追記（同日）: 21件目 —「なぜプレビュー画面通りの結果が三脚候補点に
ならないのか」を過去の履歴込みで再調査し、探索の頑健性を改善

過去の履歴（TRIPOD_ECEF_DIRECTION_FRAME_FIX_20260823.txt）を確認した
結果、"実証ケース約968m" という記述を発見した。8月23日時点で既に
このプロジェクト・この地点が既知の回帰テストケースであり、当時は
「候補地点の天体方位角/高度を、別地点のENU基底へそのまま適用していた」
ことが原因（0.0087°のずれ）と判明・修正済みだった。現在のコードを
1つずつ照合したところ、この原則（ECEF方向は必ずそれを計算した観測点
自身のENU基底で変換する）は、粗い交点探索・精密化の候補再評価・被写体
仰角計算のいずれでも正しく守られていることを確認した。

新たに判明した点: 粗い交点探索は「幾何学的高度」（大気差を含まない
理論値）でレイを構成する設計であり、最終のround-trip判定は「見かけの
高度」（大気差込み）で判定する。これはコードの明示的な設計判断であり
バグではないが、太陽高度が低い（本ケースで約7.5°）場面では、この2つの
基準の差（大気差そのもの）が無視できない大きさ（試算で約0.115°、
実機診断の仰角誤差0.096°とほぼ一致）になりうる。

この差自体は精密化の探索半径（24〜80m）に対して十分小さい実距離
（十数m程度）で補正できるはずだが、実機診断では6回の精密化を経ても
スコアが1.7〜4%までしか改善しなかった。原因として、精密化が採用して
いる「最良点を中心に半径を縮める」貪欲パターンサーチが、地形が局所的に
複雑な場所（堤防近傍等）で、真の最小値の近くにある別の（より緩い）
局所的な窪みに収束してしまい、そこから抜け出せなくなる可能性がある
ことを特定した。

修正: src/cesium/tripodCandidates.ts refineWithManualEquivalentProjection()
- 既存の貪欲探索ループの判定条件・収束条件・acceptance gate（0.5%
  round-trip）は一切変更していない。
- ループが完了した時点でまだ許容誤差（0.5%）に収まっていない場合だけ、
  「安全網」として、縮小前の元の探索窓全体を、通常パスより細かい格子
  （16分割）で一度だけ再走査する。
- この安全網で見つかった候補は、既存の貪欲収束結果より明確に良い場合
  （スコアが半分以下）だけ採用する。見つからない・改善しない場合は
  何もせず、既存の結果をそのまま使う（悪化させることは無い設計）。
- 通常パスと同じevaluateManualEquivalentCandidate/round-trip判定条件を
  そのまま使うため、判定基準自体は一切変更していない。

また、次回以降の診断で「精密化がどう収束したか」を直接確認できるよう、
各パスの中心距離・探索半径・そのパスの最良スコア・最良距離・外縁ヒット
の有無を記録し、「この検索の診断情報をコピー」に追記する診断機能も
あわせて追加した（探索の挙動・精度には影響しない）。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ、tripod-candidate-round-trip.test.mjsの8件全てPASSを
  含む）/ npm run lint、全てPASS。
- 前回（los-safe補間の直接採用）と異なり、今回は「既存の探索結果を
  絶対に悪化させない」設計（2倍以上明確に良い場合だけ採用）にしており、
  Node.js単体の数値シミュレーションでも、既に貪欲探索が成功している
  ケースに悪影響が無いことを確認した。
- 実際のGSI地形データによる、979m付近の実機再検証は未実施（ネットワーク
  制約）。次回、同じ地点・日時で再検索し、確定候補が968m付近になるか、
  ならない場合は新しい診断（精密化パスの推移）を貼っていただきたい。

追記（同日）: 22件目 —「v10に一度戻してみては」というご提案から着想を得た、
実データ検証による根本原因の確定

ご提案（V10相当への切り戻し）を検討する前に、まず現在のコードが実際に
どう振る舞うかを、このサンドボックス環境で直接検証できることに気づいた。
既存の回帰テスト基盤（tests/regression/tripod-candidate-round-trip.
test.mjsが使うmakeMockTerrainSampler）を流用し、実際の本番コード
（calculateTripodCandidates等）に対して、実際の被写体座標・日時・
カメラ設定と、実際の生データ診断（1050m〜1511m、20〜30m間隔）に基づく
合成地形を与えて、憶測でなく実行結果で検証した。

検証1（クリーンな単一勾配地形）: 幾何学的な交点がちょうど979mになる
なめらかな単一勾配の地形を与えたところ、確定候補は970.6m（約8.4mの
差）で正しく確定した。これにより、探索アルゴリズムの基本構造
（ECEFレイ・2次元精密化・round-trip判定）自体は健全であることを確認した
（V19の2D精密化やこれまでの修正を疑う必要はない）。

検証2（実在する二重交点地形）: 実際の生データと同じ傾斜（1050〜1511m）
に、968m付近に幅40〜80m規模の、確実に正負が入れ替わる（＝数学的に
確実に実在する）地形の起伏を追加したところ、驚くべきことに、この
968m付近の交点は粗探索で一切検出されなかった（3件どころか1件も）。
精密化・安全網の問題ではなく、粗探索自体がこの規模の地形を見落として
いることが実証された。

原因の特定: 粗探索の対数間隔サンプル（32点）の密度補完しきい値
（ADAPTIVE_COARSE_MAX_SPAN_METERS）が500mに設定されていた。実際の
探索窓（8m〜1200m程度）でも、対数間隔だけでは900〜1000m付近の間隔が
90〜160m程度になり、幅40〜80m規模の実在する地形の起伏（川沿いの堤防・
土手等でごく普通に見られる規模）が、2つのサンプル点の間に完全に
埋もれてしまい、交点として検出されないことが分かった。

修正: src/cesium/tripodCandidates.ts
- ADAPTIVE_COARSE_MAX_SPAN_METERS を500m→30mへ縮小した。
- 検証2と同じ合成地形で再テストしたところ、見落とされていた2つの交点
  （989m、948m）が正しく検出・確定されるようになったことを確認した。
- 典型的な探索窓（8m〜1200m程度）でのサンプル数増加は32点→62点程度
  （既存の総サンプル数上限640に対して十分小さい）。10m DEMのまま
  （1m化はしない）のため、通信量・最終的な精度条件は変更していない。

これまでの5・6・7・11・14・21件目の修正（探索窓のスライド・レンジ
サイジングの分離・安全網の広域再走査等）は、いずれも「粗探索が交点を
見つけた後、精密化がどこまで正確に収束できるか」という、この22件目
とは異なる層の問題に対処するものだった。今回の実データ検証で、実際の
ボトルネックはそれよりも手前の「粗探索がそもそも交点を見つけられて
いるか」にあったことが判明した。過去の修正が無駄だったわけではなく
（実際に別の失敗モードを塞いでいる）、根本原因はさらに手前の層に
あった、ということである。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ、tripod-candidate-round-trip.test.mjsの8件全てPASSを
  含む）/ npm run lint、全てPASS。
- 上記の検証1・2は、本番と同じcalculateTripodCandidates関数を直接
  呼び出す一時的な検証テストで実施し、確認後に削除した（恒久的な
  テストファイルとしては追加していない）。
- 実際のGSI地形データによる実機再検証は未実施（ネットワーク制約）。
  次回、同じ地点・日時で再検索し、確定候補が968m付近になるか確認
  いただきたい。

追記（同日）: 23件目 —「ダメだね、v10を参考に検証して」というご指摘への
対応：数の不整合（3件中1件しか確定せず除外理由0件）を可視化し、
V10方式との比較検証も実施

ご報告の実機診断を精査したところ、22件目の密度修正後も1252mのまま
だったが、重要な手がかりを発見した：「交点候補3件→確定1件」なのに
「除外理由: なし」（正式な棄却が0件）という、数が合わない状態だった。
これは、残り2件のいずれかで、reject()を経由しない生の例外が発生し、
Promise.allSettledの失敗経路へ落ちて、診断（最終判定詳細）に一切記録
されないままconsole.warnだけで静かに失われていたことを示唆する
（利用者には何も見えない）。22件目の密度修正で新たに見つかるように
なった交点の処理中に、想定していなかった経路で例外が起きている可能性が
高いと判断した。

また、ご提案いただいた「V10を参考にした検証」も実施した。実際の生データ
（1050〜1511m）と同じ傾斜に、968m付近に確実に正負が入れ替わる地形を
追加した合成地形に対し、本番のcalculateTripodCandidates関数を、実際の
アプリと同じ経路（距離範囲を明示指定しない、seedベースの絞り込みを使う
デフォルト経路）で直接実行したところ、22件目の密度修正込みの現行コード
は989m・948m（968mを挟む2つの正しい候補）を正しく確定できることを
確認した。これにより、22件目の修正自体は少なくとも合成地形では機能して
いることを重ねて確認した。実機で1252mのまま変化しないのは、修正が
効いていないのではなく、上記の「黙って失敗する候補」が968m側の候補で
起きている可能性が高いと考えられる。

修正: src/cesium/tripodCandidates.ts processInitialSolution()
- 関数全体をprocessInitialSolutionInner()へリネームし、外側に診断専用の
  try/catchを追加した。中止（AbortError）は従来どおりそのまま伝播させる。
- それ以外の、これまでreject()を経由せず握りつぶされていた生の例外を、
  新しい理由コード「processing-exception」として必ずfinalEvaluations
  （最終判定詳細）へ記録するようにした。
- 既存の内部のreject()呼び出し・制御フローは一切変更していない
  （壊れていなかった経路の挙動は変えていない）。この修正は「今まで
  見えていなかった失敗を見えるようにする」ことだけが目的。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- 合成地形（実際の生データ＋968m付近の確実な二重根）に対し、本番コードを
  実際のアプリと同じデフォルト経路で実行し、989m・948mが正しく確定
  することを確認した（一時的な検証用テストで確認後、削除済み）。

次回のお願い
- 同じ地点・日時で再検索し、「この検索の診断情報をコピー」を貼って
  いただきたい。今回の修正により、もし968m側の候補が何らかの例外で
  失敗している場合は、「最終判定詳細」に
  「processing-exception」という理由と共に必ず表示されるはずである。
  これが表示されれば、次はその具体的な例外原因を特定する段階に進める。

追記（同日）: 24件目 —「8月7日時点では正常だった」というご報告を受けた
歴史的コードの直接比較検証と、決定的な安全策の復元

ご提供いただいた2026-08-07前後のコード一式（AR camera phase6、
PRECISION_STAGE2_HEIGHT_RESOLVER_20260807.md等を含む）を、v8・現在の
コードと同様に実際に動かして比較した。

決定的な違いを発見: この時期の三脚候補探索（scanTerrainDistanceRange）
は、v8・現在のECEFレイ方式とは根本的に設計が異なっていた。

- v8・現在: 「符号が反転する交点（明確な交差）が1つも見つからなければ、
  候補ゼロを返す」
- 2026-08-07頃: 「交差が見つからなくても、その時点で最も0に近かった
  サンプルをそのまま後段の精密化（手動ピン相当の詳細探索）へ渡し、
  そこから真の解へ近づける」

実際にこの2026-08-07頃のコードを、今回問題になっている実地形パターン
（生データ1050〜1511mの実測傾斜＋968m付近に加えた地形起伏）で再現
テストしたところ、密度の調整を一切行わなくても948.9mを正しく発見・
確定できることを確認した。この「見つからなくても最も近い点から
精密化する」安全策が、後のECEFレイ・複数交点対応への書き直しの
どこかで失われ、それ以降（v8・現在）はこの安全策を持たないまま
今日に至っていたと判明した。

修正: src/cesium/tripodCandidates.ts scanRayTerrainIntersections()
- 符号反転する交点（bracket）が1つも見つからなかった場合、従来は
  空配列を返して該当天体の候補探索を打ち切っていた。
- 2026-08-07頃の設計を踏襲し、交点が見つからない場合は、粗探索の
  サンプルの中で最も0に近かった1点を、精密化前の初期候補として返す
  よう変更した。
- 重要: 最終的な確定・棄却の判定条件（round-trip 0.5%許容誤差）は
  一切変更していない。この「最も近い点」が実際には不正解であれば、
  従来どおり精密化・round-trip判定で正しく棄却される（偽陽性を
  許すものではない、あくまで「候補を最初から諦めない」ための変更）。
- 22件目の密度修正（500m→30m）と合わせた二重の安全策として機能する。
  実際に、22件目の密度修正だけでは検出できない、さらに狭い（幅約10m）
  地形起伏を用いた再現テストでも、この2つの修正の組み合わせにより
  968m付近（974.8m・961.3m）の候補が正しく発見・確定されることを
  確認した。

検証
- npm install後、npx tsc -b / npm run build / npm run test:regression
  （33グループ）/ npm run lint、全てPASS。
- 2026-08-07頃の実際のコードを直接実行し、密度修正なしでも948.9mが
  正しく確定することを確認（検証用の一時テストで確認後、削除済み）。
- 現在のコードに今回の修正を適用し、幅約10mという極めて狭い地形起伏
  でも974.8m・961.3mの候補が発見・確定されることを確認（検証用の
  一時テストで確認後、削除済み）。
- 実際のGSI地形データによる実機再検証は未実施（ネットワーク制約）。
  次回、同じ地点・日時で再検索し、確定候補が968m付近になるか確認
  いただきたい。
