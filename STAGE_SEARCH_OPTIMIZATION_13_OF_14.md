# 検索高速化 第13/14段階

## 実装内容

- 端末の論理CPU数と公開されているメモリ目安から、検索候補計算と最終3D LOS確認の同時実行数を保守的に自動決定
- Pro精度では同時実行数を1段階抑え、発熱とメモリ圧迫を軽減
- Google実景メッシュの同一LOS要求をViewer単位でPromise共有
- 同一三脚位置・レンズ中心高のLOS観測点準備をViewer単位で共有
- LOSキャッシュは上限付きで管理し、失敗した計算は削除して再試行可能
- 中断要求は共有計算そのものを破棄せず、呼び出し元ごとに独立して反映

## 精度維持

- 天体座標、DEM、Karney測地計算、LOS方向、遮蔽閾値は変更していない
- キャッシュキーは計算に使用した数値を丸めず、そのまま使用
- 計算点数や候補数を削減せず、完全に同一の要求だけを再利用

## 検証

- ZIP構造検査を実施
- 完全ビルドは元プロジェクトに `vite/client` と `@types/node` の実体が含まれないため依存解決段階で停止
