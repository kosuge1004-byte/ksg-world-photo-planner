# AstroSight 修正・検査報告（2026-09-19）

対象: AstroSight-latest-20260910.zip。元のZIPは変更せず、修正版を別ファイルに作成しました。ZIP内の過去の引き継ぎ文書は参考資料として扱い、今回の依頼を作業方針にしました。

## ダウンロードが失敗する原因と修正

| 確認した原因 | 修正 |
| --- | --- |
| 標高キャッシュが取得元を保存せず、再利用したGSI標高がWorld Terrain扱いになる。明示ダウンロードの品質チェックで拒否される。 | 標高・ジオイド高・取得元を保存して復元。出典不明の旧キャッシュは明示ダウンロード時に再確認する。 |
| ジオイド要求の待ち行列まで短い制限時間に含め、正常な取得をタイムアウトにする。時間切れ後も通信が残る。 | 地域ジオイドの通信期限を枠取得後に開始。中止・期限切れで待機と通信を止め、共有要求の他利用者は維持する。正常な方位処理を一括45秒で打ち切る処理を撤去。 |
| 保存用DEMタイルの取得が通常の先読み24枚制限で切られる。保存失敗や通信失敗が成功扱いになる。 | 明示保存時は必要タイルを全件対象にし、実際の保存数と失敗数を集計する。失敗した保存を再試行可能にする。 |
| 未取得の下位DEMまで参照一覧に登録し、直後の検査で「期限切れ」と誤判定する。必要方位数も常に360本と判定する。 | 実在するタイルだけ登録し、緯度に応じた必要方位数で検査する。参照情報の読み取り失敗時には既存情報を保持する。 |
| 水面だけの取得に通常の周辺情報APIを使い、8点ずつ大量に通信する。 | water-onlyを指定し、API上限の80点までまとめて取得する。点の順序と値は保持する。 |
| 中止後の後片付けと同一スポットの再保存が重なり、保存管理情報に干渉する。端末DB接続の失効後に再実行しても復旧しない。 | 後片付け終了まで同時実行を防ぐ。失効した周辺情報DB接続を破棄して再接続する。更新開始後は成功までpartialとし、中止・失敗時に古い完了表示を残さない。 |

容量確認・DB読み書き・保存用タイル取得にも期限を設けました。取得や書き込みの失敗を0mの仮標高で埋めたり、「保存完了」にしたりしません。

## 精度を維持した速度改善

- 同一地点の標高・ジオイド・保存済みプロファイルを正しく再利用し、再取得を削減。
- 周辺情報のDB接続を共有し、多点読み出しと容量検査を同一トランザクションへまとめる。
- 水面情報は8点から最大80点のバッチへ変更。
- 明示ダウンロードでは採用しないWorld Terrainへの追加通信を省略。通常検索のフォールバックは維持。
- 地域ジオイド取得の通信を整流し、サーバーの再試行をクライアントで重複させない。

地点別ジオイド補正は地域ダウンロードの待機枠と分離しました。既存の1.2秒制限内にサーバーの地点別キャッシュへ到達できることを、地域要求の処理中と地域キャッシュミス直後の両方で検査しています。

元ZIPとのSHA-256比較で、次の中核8ファイルは完全一致しました。

- src/cesium/tripodCandidates.ts
- src/cesium/celestial.ts
- src/geodesy/karneyGeodesic.ts
- src/geodesy/adaptiveTerrainProfile.ts
- src/geodesy/terrestrialRefraction.ts
- src/apparent/apparentElevation.ts
- server/gsiElevation.ts
- src/cesium/gsiElevationClient.ts

必要方位の計算、方位1°、粗探索の最大間隔30m、最詳細1m DEM要求、DEMの採用順序、中立補間、地点別・地域別のジオイドキー、候補の再精密化、収束許容0.002°を維持しています。

## ハンバーガーメニュー

地形陰影を3D表示の設定へ移動し、精度設定から整理。フリーズ診断は地図出典内に折り畳みました。2D地図の出典をOpenFreeMap・OpenStreetMap・国土地理院へ修正し、Google共有リンクの説明を区別しました。アプリ追加の表記を簡潔にし、外側クリックとEscapeで詳細パネルも閉じるようにしました。現行機能につながる入口は保持しました。

## 検査結果

| 検査 | 結果 |
| --- | --- |
| verify-*.mjsの個別109本 + 集約2本 | 111/111成功 |
| npm test | 54組すべて成功 |
| 今回追加した実処理テスト | ダウンロード13件 + DB復旧・検査5件、すべて成功 |
| 三脚候補の往復計算・複数交点・高さ基準 | 8件すべて成功 |
| TypeScript・本番ビルド | 成功 |
| lint | エラー0。既存箇所などの警告16件あり |
| Chrome、幅430pxの操作検査 | 保存・更新・中止後の再実行・水面API503時のpartial表示・メニュー・Escape・出典表示、成功。ページ例外0 |
| 実際の国土地理院API | 緯度35.36/経度136.81、北方向10km・352点の取得と同値の再利用に成功 |
| Android | Capacitor同期とManifest・プラグイン等の静的検査に成功。同期資材とdistの一致を確認 |
| 配布ZIP | 収録ファイルを展開読み取りし、元の修正済みファイルとの一致を確認 |

ブラウザの全方位保存検査は通信モックを使用し、必要259方位・14,245点（範囲1km）を保持しました。再更新では標高APIの再取得0回。実API検査は1方位の地形取得で、初回21.765秒・再利用0.004秒・再利用時通信0回でした。数値はこの検査環境の値で、実端末の所要時間を保証するものではありません。

既存の検査スクリプトのうち、旧仕様を決め打ちしていた項目、欠落した過去報告書を要求する項目、Windowsで実行できない起動処理を現行仕様へ修正しました。計算結果の回帰テストの期待値は緩めていません。

Cloudflareへの実配備、実APIでの全方位保存、Android実機・APKビルドは未検証です。ビルドには500kB超のチャンク警告が残ります。Androidのソースと同期済みWeb資材を同梱し、APKは含めていません。

## 内容・再検査

修正版ZIPは元ZIPと同じルート構成です。ソース、依存関係ロック、本番dist、Androidソース・同期済み資材、新テスト、この報告、evidence/fix-audit-20260919内の検査記録を含みます。node_modulesと作業用ログ・一時ファイルは含めていません。

展開後は以下で主要検査を再実行できます。

```text
npm ci
npm test
npm run lint
npm run build
npm run android:sync
npm run verify:android
```

本番反映にはフロントエンドとPages Functionsの両方を含む再ビルド・配備が必要です。
