# 画角内検索 最接近時刻への修正

## 修正内容

- 画角内へ入った時刻を結果として返す処理を廃止。
- 天体が画角内にある連続区間ごとに、被写体中心との球面角距離を追跡。
- 各区間で角距離が最小になる候補時刻の前後を再探索し、約1秒精度まで絞り込み。
- 画角内へ入った時刻・画角外へ出た時刻は結果に表示しない。
- 検索時間帯や曜日の境界で区間が終了した場合も、その区間の最接近時刻を確定。
- 結果は日時順で返す。

## 維持した仕様

- 三脚ピン固定。
- 被写体ピン固定。
- 結果選択時は日時のみ変更。
- 「被写体方向を横切る時刻」モードは従来ロジックを維持。

## ビルド確認

`npm run build` を実行しましたが、このZIPには `node_modules` が含まれておらず、環境上 `npm install` を完了できなかったため、`vite/client` と `node` の型定義不足で停止しました。今回変更したソースの構文上の問題によるエラーではありませんが、依存関係を導入した環境での最終ビルド確認が必要です。
