# 検索最適化 第6/14段階

## 実装内容

国土地理院DEM PNGを取得した直後に、RGB値を標高センチメートルの `Int32Array` へ一度だけ変換するよう変更した。

- タイルごとのPNG展開は従来どおり1回のみ
- 各標高点の参照時にRGBから標高値を再計算しない
- NoData値は専用センチネルで保持
- 標高値、タイル座標、画素座標、DEM優先順位は変更なし
- 共有PromiseおよびLRUタイルキャッシュを維持

## 精度

国土地理院DEMの元データが1cm単位で符号化されているため、整数センチメートルで保持し、返却時に0.01倍してメートルへ戻す。従来の復号式と同一であり、精度低下はない。
