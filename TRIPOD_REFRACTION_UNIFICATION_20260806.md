# 三脚候補探索とプレビューの仰角計算統一

## 修正内容

- `src/cesium/tripodCandidates.ts` の独自ECEF幾何仰角計算を廃止。
- 撮影プレビューと同じ `calculateElevationAngleDegrees()` を使用。
- 三脚候補探索、精密化、前回解再利用判定、最終画角判定のすべてで、同一の地表屈折補正を適用。
- 方位角、DEM取得、Karney測地線、天体計算、PLATEAU表示処理は変更なし。

## 解消対象

探索時には被写体の幾何仰角、プレビュー時には地表屈折補正済み仰角を使っていた不整合を解消する。
