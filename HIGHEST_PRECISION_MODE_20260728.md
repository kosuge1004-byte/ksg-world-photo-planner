# 最高精度モード

## 動作

- ハンバーガーメニューの「精度設定」で「標準（初期値）」と「最高精度」を選択できます。
- 標準モードの検索・三脚配置・プレビュー・遮蔽物判定は従来経路のままです。
- 最高精度処理は、スポット検索結果の「三脚ピンを置く」を押した後だけ実行されます。
- 処理中は進捗バーを表示します。
- 必要なデータを取得できない場合は標準へ切り替えず、「高精度データを取得できませんでした」と表示します。

## 最高精度の後処理

1. 元候補の半径20m以内を5m刻み相当で局所再探索。
2. OSM情報で歩行不可・立入制限・車道上の候補を除外。
3. 全候補で距離によるDEM詳細度低下を行わず、利用可能な最高詳細度を要求。
4. 0.01度地域共有ではなく各座標でジオイドを取得。
5. 三脚・被写体をGoogle Photorealistic 3D Tilesの最詳細表面へクランプ。
6. 三脚レンズ中心・被写体をECEF座標化し、有限線分の3D遮蔽物を再判定。
7. 条件を満たす候補のうち元検索結果に最も近い地点を最終三脚位置として確定。
8. 最終座標を適用してから既存プレビューを再描画。

## 気象ベース大気差の一貫性

- 最高精度モードの日時検索で使用したOpen-Meteoの予報／平年気象コンテキストを、選択後の本編へ引き継ぎます。
- プレビューの太陽・月・天の川・北極星、日周軌跡、天の川帯、タイムラインの日出没・月出没・天の川撮影可能時間が、検索と同じ地点・時刻別Bennett補正を使用します。
- 補正済み水平座標を既存の3D遮蔽物判定にも共有するため、表示位置とレイキャスト方向が一致します。
- 最高精度かつ自動補正で必要な気象データを取得できない場合、標準大気へ自動変更せずエラーを表示します。

現地の樹木、工事、仮設物など、取得データに存在しない障害物は保証対象外です。
