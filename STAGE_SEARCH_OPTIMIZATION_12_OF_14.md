# 検索最適化 第12/14段階

## 局所探索＋全範囲フォールバック

- 近接方位で得た前回の三脚距離を中心に、65〜135%の局所範囲を先に探索するよう変更。
- 前回距離そのものが高度角誤差0.002度以内なら即時再利用。
- 局所探索結果も高度角誤差0.002度以内の場合だけ採用。
- 局所探索が収束しない場合は、必ずユーザー指定の全距離範囲を従来と同じサンプル数・精密化回数で再探索。
- 局所探索と全範囲探索を三脚候補計算側へ統合し、呼び出し側の二重実行を解消。

検索範囲、DEM精度、Karney測地計算、画角判定、収束閾値は変更していないため、精度を維持したまま近接日時の探索を高速化する。
