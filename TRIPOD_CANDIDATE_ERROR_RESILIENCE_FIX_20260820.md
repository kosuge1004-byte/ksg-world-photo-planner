# 三脚候補計算エラー多発対策 (2026-08-20)

## 原因
1. GSI DEM/ジオイドが一部利用できない場合の Cesium World Terrain フォールバックに再試行がなく、一時的な通信失敗が即座に候補計算エラーになっていた。
2. 複数天体の候補計算を `Promise.all` で束ねていたため、太陽・月・天の川等のうち1件だけ地形取得に失敗しても全候補が失敗扱いになっていた。

## 修正
- Cesium World Terrain の `sampleTerrainMostDetailed` を同一条件で1回だけ再試行。精度・データソースは変更しない。
- 候補計算を `Promise.allSettled` に変更し、天体ごとの失敗を分離。
- 一部候補だけ失敗した場合は成功候補を維持。
- 全候補が地形取得エラーになった場合のみ全体エラーを返す。
- Abort は従来どおり即時伝播。

## 精度への影響
なし。同一座標・同一DEM・同一計算条件を再試行するだけで、代替高度や近似値は使用しない。
