# スポット検索追加高速化

- 被写体確定直後にサーバー側で24方位・各12点の粗DEMを先行取得
- 国土地理院タイルキャッシュ上限を128から512へ拡張
- 標準モードの候補計算並列数を2から3へ調整（Proは2を維持）
- 表示件数と精度モードに応じて精密化ウィンドウを動的調整
- 検索進捗を5相として明示
- 既存の局所探索、全範囲フォールバック、見通し後段化、結果日時順を維持

Web Worker化は、現在のスポット検索本体がサーバーバックグラウンドジョブで動作しているため対象外。メインUIスレッドから既に分離されている。
