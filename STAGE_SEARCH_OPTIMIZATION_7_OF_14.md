# 検索最適化 第7/14段階

## 実装内容

展開済み国土地理院DEMタイルをNetlify Blobsへ永続保存する処理を追加した。

- メモリキャッシュにない場合、先に永続キャッシュを確認
- 永続キャッシュにもない場合だけGSI PNGを取得・展開
- 展開済み `Int32Array` をバイナリ形式で保存
- 幅・高さ・形式バージョンをヘッダーへ保存し、読み込み時に厳密検証
- 同一タイルの重複書き込みをPromise共有で抑止
- Blob未設定・読み書き失敗時は従来の通信処理を継続
- 永続化の失敗を検索失敗として扱わない

## 精度

標高値、NoData値、画素位置、DEM優先順位、復号式は変更していない。
保存対象は第6段階で生成した整数センチメートル配列そのものであり、再検索時の精度は同一である。
