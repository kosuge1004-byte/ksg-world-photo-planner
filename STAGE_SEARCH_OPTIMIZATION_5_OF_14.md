# 検索最適化 第5/14段階

## 実装内容

- DEM標高点を点ごとに逐次処理せず、標高種別ごとに必要タイルを集約。
- 同一バッチ内の重複タイルを除外し、必要なタイル群をまとめて取得。
- 取得済みタイルから同一タイル内の複数標高点を連続して抽出。
- DEM1A → DEM5A → DEM5B → DEM5C → DEM10B の優先順位を維持。
- 1m・5m・10mの詳細度制約を各標高点ごとに維持。
- 日本国外の座標、中断処理、2,048点上限、既存タイルLRUキャッシュを維持。

## 精度への影響

標高種別の優先順位、座標から画素への変換、PNG復号、標高値復号は変更していません。取得と待機の単位だけを点単位からタイル単位へ変更しています。

## 変更ファイル

- `server/gsiElevation.ts`
- `STAGE_SEARCH_OPTIMIZATION_5_OF_14.md`
