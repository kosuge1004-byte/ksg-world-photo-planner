# Phase6-1 LOS高速化

## 実装

- DEM地形LOSとOSM建物・植生LOSを直列実行から並列実行へ変更。
- 地形プロファイル走査でサンプルごとに生成していた `Cartesian3` を再利用し、短命オブジェクトとGC負荷を削減。
- 地形サンプル数と距離配列数が不一致の場合に短い方へ合わせる安全策を追加。
- OSM LOSの共有Promiseへ個別リクエストの `AbortSignal` を渡さず、1件のキャンセルが同一キーの他リクエストへ波及しないよう修正。
- 呼出元単位のキャンセル応答は `awaitWithAbort` で維持。

## 性能上の効果

ネットワーク条件に依存するため固定の短縮率は断定しない。少なくとも、未キャッシュ時のDEM待ち時間とOSM待ち時間が加算される直列構造を解消し、長い方の待ち時間を中心とする並列構造へ変更した。

## 検証

- `npm run verify:phase6-1`
- `npm run build`
- `npm run test:regression`
