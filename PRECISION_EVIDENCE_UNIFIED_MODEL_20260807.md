# 精度エビデンス統一モデル（2026-08-07）

## 実装方針

- 測地線: GeographicLib/Karney WGS84逆解を唯一の方位・距離基準とする。
- 3次元位置: Cesium WGS84 ECEF。高さは楕円体高へ統一してから使用する。
- 日本の標高変換: 国土地理院ジオイドモデルを介し、標高と楕円体高を明示的に区別する。
- 天体: Astronomy Engineの観測地点依存（topocentric）経路を使用する。
- カメラ: `createUnifiedCameraModel()` を実カメラ、天体オーバーレイ、人物投影の唯一の基準とする。
- 3D表面高度: 三脚・人物・被写体を3D画面上でユーザーが明示選択した場合だけ正式値として保持する。
- 2Dクリック、検索、URL、座標入力、自動候補: 共通DEM/ジオイド経路を使用し、表示用3Dレイヤーの高さを混入させない。

## 地表屈折

地上気象の気温・気圧・湿度だけでは、地表屈折係数kに必要な鉛直屈折率勾配を一意に決められない。そのため、根拠のない動的k推定は実装しない。現在は標準値k=0.13を明示し、不確かさを別管理する。鉛直温度勾配または屈折率プロファイルが利用可能になった場合のみ測定勾配モデルへ切り替える。

## 今回の統一

- 新規 `src/cesium/cameraModel.ts`
- 新規 `src/cesium/optics.ts`
- Cesium実カメラ、天体投影、人物投影を同じ方位・仰角・FOV・ECEF基底へ統一
- 2D被写体配置の暫定0m/旧高度計算を廃止
- 2D三脚配置で3D表面クランプを行わない
- 検索被写体はDEM/ジオイド経路へ統一
- 3D明示選択時のみ選択表面高度を保持
