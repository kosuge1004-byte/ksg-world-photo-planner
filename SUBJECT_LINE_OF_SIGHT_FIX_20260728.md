# 三脚－被写体間 3D遮蔽物確認修正

- 遮蔽物確認のレイ方向を、検索結果の天体方位・高度から、三脚レンズ中心→被写体ピンの3次元方向へ変更。
- 天体用の地平線以下判定と天体視直径サンプリングを使わず、被写体までの有限線分専用判定を追加。
- 被写体までの距離帯別除外値（3m / 10m / 20m / 50m、およびユーザー変更値）は継続使用。
- 被写体より奥の交差は判定対象外。
- 検索メニューのON/OFF、設定保存、結果表示は既存仕様を維持。

## 検査

- 変更2ファイルのTypeScript構文変換検査: 成功。
- `tsc -b`: ZIPにnode_modulesが含まれず、vite/client・node型定義が不足するため完了不可。
