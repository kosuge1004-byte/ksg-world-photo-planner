
## 2026-07-22
- 三脚場所検索を太陽・月のみに限定。
- 中心一致0.05°判定を廃止し、指定焦点距離・センサー・プレビュー縦横比で円盤全体が画角内に収まるかを判定。
- プロ／標準モード切替UIを廃止し、大気差（屈折）補正を常時適用。
- 2D地図で1本指パン中に2本目を置いた際、パン量を中心座標へ確定してからピンチへ移行するよう修正。

## 2026-07-22 地面投影ライン／三脚探索基礎ライン
- 地面投影ラインを将来の三脚候補・撮影可能区間・遮蔽物・高度判定で共有する `TripodSearchBaseLine` として分離。
- 表示中の各天体について、被写体から天体方位の反対側へ延びる地表ラインをリアルタイム更新。
- 2D地図と3D地図で同じ基礎ラインデータを使用。
- 三脚候補専用ラインを廃止し、候補点のみを共通基礎ライン上へ表示して二重描画を防止。
- Cesium側は `CallbackProperty` の座標だけを差し替え、時間軸操作中にラインEntityを破棄・再生成しない構造へ変更。

## 三脚ポイント検索：距離範囲指定
- 被写体から三脚候補までの距離を0〜10,000mで指定可能にしました。
- 初期値は500〜3,000mです。
- 最小・最大それぞれのスライダーと数値入力を同期しました。
- 設定値をlocalStorageへ保存し、次回起動時に復元します。
- 距離範囲を三脚候補の地形探索そのものへ適用し、範囲外の候補を除外します。

## 2026-07-22 天体通過日時検索
- 日時欄の横に天体通過日時検索ボタン（🔍）を追加。
- 現在表示中の太陽・月・天の川を対象に、被写体方向と方位が一致する日時を検索。
- 30日・90日・180日・365日・指定期間に対応。
- 三脚ポイント検索と曜日選択・表示件数を共通部品化し、同一UI・同一挙動を使用。
- 結果を曜日付きの時系列一覧で表示。
- 結果選択時に既存の日時変更処理を通して2D・3D・プレビューを更新。

## 2026-07 UI correction
- Fixed Spot Search content overflow by applying border-box sizing and a responsive subject-search row.
- Kept the celestial transit search launch button beside the date/time control and verified the search opens as a separate modal dialog.
- Moved “3Dで被写体を指定” directly below the subject/tripod pin controls.
- Collapsed the pin drawer while placing a foreground/midground object so the map remains clickable.
- Unified foreground-object text sizes to the same intermediate UI scale.
- Added a range slider plus numeric input for person height (50–300 cm).
- Changed the Cesium person billboard to physical meter sizing and removed the oversized preview minimum width.

## 2026-07-22 UI修正・再検査

- スポット検索の入力欄と3個の履歴／お気に入りボタンを2段の可変グリッドへ変更し、320px幅からPC表示まで横方向にはみ出さないよう修正。
- 天体通過日時検索ボタンをプレビュー上から削除し、時間軸の日付ボタン横へ移動。
- ピン設定を「被写体ピンを変更」→「3Dで被写体を指定」→「三脚ピンを設置」の順へ変更。
- 前景・中景オブジェクト欄の見出し、補足、ボタン、入力欄を中間の文字サイズへ統一。
- 人物配置中は大きなピン設定メニューと地図操作ボタンを非表示にし、右上に小さな「配置を中止」ボタンのみ表示。
- 2Dで人物を配置した直後に配置モードを自動終了し、高さ変更とドラッグ移動をすぐ行えるよう修正。
- 人物の高さ入力を、スライダーと50〜300cmの数値入力で変更できるよう改善。数値欄は入力途中の空欄を許容し、確定時に範囲内へ補正。
- 3D人物を地形標高上の実寸メートル表示へ統一し、常時前面表示を解除。
- プレビュー人物の最低表示サイズ補正を撤廃し、焦点距離・距離・身長から計算した角サイズをそのまま反映。

## 2026-07-22 再検査で判明した不具合の修正
- 前景人物プレビューの画角計算を、アプリ本体と同じフルサイズセンサー寸法計算へ統一。縦構図・画面比率時の人物サイズ誤差を修正。
- 3D人物のドラッグ処理をEntity更新処理から分離し、移動のたびにドラッグハンドラが解除される問題を修正。
- 天体通過日時検索ボタンの記号を仕様どおり「🔍」へ変更。

## 2026-07-22
- アプリ起動時の表示日時を、前回保存日時ではなく端末の現在日時で初期化するよう修正。

## 2026-07-23 天の川描画切替
- 3Dプレビュー: 見通せる天の川を写真調の帯で表示し、見通せない部分は暗い円形位置マーカーのみ表示。
- 2Dマップ: 天の川の帯表示を円形マーカー列へ変更。見通せる位置は白、遮蔽位置は濃灰で表示。
- 既存の天文計算、日時計算、軌道計算は変更せず、描画レイヤーのみ変更。

- プレビュー画面で前景被写体が天体より手前に描画されるよう、前景レイヤーの重なり順を修正。2Dマップ・天体計算は変更なし。

## 2026-07-23 月齢カレンダー（オフライン天文計算版）
- ハンバーガーメニューに「月齢」を追加。
- 月単位のカレンダーで、各日の月相画像・月齢を表示。
- 日付選択時に月相名、月齢、照明率を詳細表示。
- `astronomy-engine` の月位相・照明計算と直前の朔検索を使用し、外部API通信なしで動作。
